# 屏幕控件与 UI

> 知识树：用户说明部分已录入 `knowledge/game-engine-knowledge/camera-ui-rules.md`（标注未验证，2026-08-08，bnd_7320a9bd）

> 状态：框架草案
> 来源：用户对屏幕控件、输入和按钮事件的说明
> 最近校验：2026-08-01
> 适用范围：屏幕空间 UI、控件身份、显示状态和输入事件

屏幕控件属于 UI 系统，不存在于关卡的 3D 空间中。无论玩家如何移动或转动视角，控件都显示在玩家看到的长方形屏幕坐标空间里。

## UI 框架总览（2026-08-22 用户补充 + 差分闭合）

> 状态：已验证（用户口述框架 + 真实 .gil 相邻快照差分互证）
> 来源：用户 2026-08-22 口述 UI 框架 + 地图 1073741907 四轮差分。

### 概念层级（用户口述）

```
素材（素材库定义）
  ├── 游戏内置基础图形（三角形/圆形/矩形等），可单独或几张合成一个素材
  └── 用户自定义素材（AI 生成 → 素材库注入）
        ↓ 被引用
图片控件（引用具体素材）
        ↓ 创建为
控件模板（可复用）
        ↓ 被引用
界面布局（玩家屏幕看到的一层视觉）
  ├── 默认布局：官方预置（小地图/技能区/队伍/生命条/退出/网络/聊天等）
  └── 用户布局：引用官方模板或自制模板，选摆放位置
        ↓ 被控制
节点图（控制用哪个布局、布局里哪个控件开/关）
```

官方支持的控件：文本框、图片、特殊数字、交互按钮、自定义按钮、弹窗、计时器等。

### wire 结构（root9 三段，差分闭合）

root9 同时承载三段，都是 field 502 记录，按记录结构区分：

| 段 | 记录 | 关键字段 |
| --- | --- | --- |
| 素材段（素材库） | 容器/组/图元 | 容器 ID = 素材索引 ID（`0x40000000+`） |
| 控件段 | 模板 + 实例 | 见下 |
| 布局段 | 默认布局 + 控件组容器 | `f503` = packed 控件实例 ID 列表 |

### 控件模板 ↔ 实例 双向引用

| 方向 | 路径 | 说明 |
| --- | --- | --- |
| 实例 → 模板 | `f502[type3].f13.f501` | 实例引用模板 ID |
| 模板 → 实例 | `f502[type4].f14.f501` | packed 实例 ID 列表（back-reference） |

- 模板：`f502[type4]` 自标识，无 `f504`，含图片源 `f505[f502=38]`。
- 实例：`f502[type3]` 引用模板，`f504` = 布局/容器 ID。

### 官方预制控件 vs 自定义模板（2026-08-22 第五轮差分闭合）

| 路径 | 记录数 | 结构 |
| --- | --- | --- |
| 自定义模板 | 2 条（模板 + 实例） | 模板 `f502[type4]` 无 `f504`；实例 `f502[type3]` `f504`=布局 |
| 官方预制控件 | 1 条 | `f502[type5]` 单条，`f504`=布局，直接含图片源 `f505[f502=38]` |

**图片源引用素材路径两者完全一致**：`f505[f502=38].f503.f31.f6.f4` = 素材索引 ID。
官方预制控件（文本框/按钮/图片等）直接单条记录进布局，无需模板/实例分离；
自定义模板才需要「模板 + 实例」两条记录。

### 布局引用规律

「把控件模板放进布局」= 三步联动：

1. 新建实例记录（`f504`=布局 ID，`f502[type3]` 引用模板）；
2. 实例 ID 追加进布局的 `f503` packed 列表；
3. 模板的 `f502[type4].f14.f501` 追加实例 ID（back-reference）。

- 默认布局 `1073741825`：`f503` = 14 官方预置（1073741826~1839）+ 用户新增实例。
- 控件组容器 `1073741840`：`f503` = 尚未放入布局的实例列表。

### 节点图控制（待差分）

节点图可控制「用哪个布局」和「布局里哪个控件开/关」——这是下一缺口，需节点图差分闭合。

## 与 3D 世界的区别

```text
3D 世界
├── 实体有位置、方向和旋转
├── 玩家移动会改变观察视角
└── 世界内显示可能被遮挡或需要朝向玩家

屏幕控件
├── 位于固定的屏幕空间
├── 不使用 3D 世界位置
├── 不随摄像机视角改变空间关系
└── 直接接收或展示玩家界面输入与内容
```

屏幕控件和实体上的铭牌虽然都能显示内容，但不是同一套系统。铭牌仍属于 3D 世界，见[组件](components.md)。

## 配置与身份

控件需要预先添加和配置。每个控件拥有自己的控件 ID，游戏运行时通过该 ID 操作指定控件。

```text
编辑器中添加控件
→ 配置控件内容和行为
→ 获得控件 ID
→ 游戏运行时按 ID 操作
```

## 运行时状态

游戏中可以对控件执行显示状态操作，例如：

- 启用；
- 显示；
- 隐藏；
- 关闭；
- 其他待逐步确认的状态变化。

“隐藏”“关闭”和“禁用”是否保留内部状态、是否继续接收输入，需要分别验证，不能暂时视为同一种操作。

## 输入与响应

控件用于接收或表达玩家界面的输入，包括：

- 按钮点击；
- 键盘输入；
- 鼠标输入；
- 在指定屏幕区域显示文字、图片或其他内容。

这些能力都需要先在编辑器中配置好，再由运行时接口控制。

## 按钮组件

控件可以包含预置的按钮组件。玩家按下按钮后，事件首先发送给该玩家对应的角色，而不是任意关卡实体。

```text
玩家按下 UI 按钮
→ 角色收到控件按钮事件
→ 角色挂载的节点图处理事件
```

这是 UI 与普通实体组件事件的重要区别：屏幕控件跟随玩家体验，而角色是稳定承接该玩家 UI 事件的对象。

## 转发到其他实体

如果按钮最终需要控制其他实体，可以在角色节点图中增加转发逻辑：

```text
按钮被按下
→ 角色收到事件
→ 角色节点图确定目标实体
→ 转发信号或调用目标逻辑
→ 目标实体执行操作
```

具体采用信号、实体事件还是其他 API，需要按目标功能和真实节点规则选择。

## 多人边界

每个玩家拥有自己的视角、输入和角色。因此 UI 的显示状态、按钮事件和转发逻辑是否按玩家隔离，是后续必须验证的核心规则。

不能因为一个玩家看见或点击了控件，就默认所有玩家都发生相同变化。

## 与其他知识的关系

- 玩家实体和角色实体的创建及多人执行，见[节点图挂载与生命周期](graph-mounting.md)。
- 按钮事件进入节点图后的执行路径，见[控制流](control-flow.md)。
- 事件携带的数据和目标实体传递，见[数据流与连接](data-flow.md)。
- 3D 世界中的铭牌与选项卡，见[组件](components.md)。
- UI 触发的玩家视角变化，见[镜头系统](camera-system.md)。
- UI 事件在整局游戏中的位置，见[游戏运行生命周期](game-lifecycle.md)。

## 图片控件引用素材的 wire 编码（2026-08-22 差分闭合）

> 状态：已验证（真实 .gil 相邻快照差分）
> 来源：地图 1073741907（ui-inject-diff）before `f27984bc`（86734B）→ after `14902676`（87356B），
> 唯一变化 = 添加一个「图片控件」，图片源=静态引用，引用素材 guide-tap（容器 ID 1073741927）。
> 快照：`~/genshin-ts-evidence/ui-control-image-ref/exp1/raw/`。
> 证据层级：真实编辑器观察；**尚未游戏验证**（图片是否真的显示待核验）。

### 记录结构：图片控件 = 模板 + 实例 两条记录

添加一个图片控件会在 root9 新增**两条** 502 记录（与素材库「容器+分类副本」同构，但语义是「模板+实例」）：

| 记录 | 特征 | 说明 |
| --- | --- | --- |
| 模板（definition） | 无 `f504`；`f502[type4]` 自标识 | 图片控件的本体，含图片源配置 |
| 实例（instance） | `f504` = 布局 ID；`f502[type3]` = `{f13={f501=模板ID}}` | 引用模板，注册进布局 |

实例通过 `f502[type3].f13.f501` 指向模板 ID；模板通过 `f502[type4]` 自标识。

### 图片源引用素材的 wire 路径（核心）

两条记录都含图片源组件 `f505[f502=38]`，素材索引 ID 存在：

```
f505[f502=38]            ← 图片源组件（type 38）
  f503
    f31 (msg)            ← 图片源 = 静态引用
      f3 (msg) = {f501: -1}   ← 无颜色覆盖（-1 = 默认）
      f4 (varint) = -1        ← 无颜色覆盖
      f6 (msg) = {f2:1, f3:8, f4:<素材索引ID>}  ← 素材引用
      f10 (bytes 0B)
    f501 = 22
    f502 = 38
    f503 = 1
    f504 (msg) = {f2:1, f3:8, f4:<自身ID>}  ← 自引用
```

**关键结论**：素材索引 ID（= 素材库容器 ID，`0x40000000+` 段）存在
`f505[f502=38].f503.f31.f6.f4`。`f6` 是素材引用结构 `{f2:1, f3:8, f4:素材ID}`。

> 两个独立样本确认（2026-08-22）：`f6.f2=1`、`f6.f3=8` 是**固定常量**（非图片源/拉伸类型编码）；
> `f3={f501:-1}`、`f4=-1` 是「无颜色覆盖」标记（两个样本一致）。

### 与素材库注入的衔接

- 素材库注入（`assets:library-inject`）写入的容器 ID = 素材索引 ID；
- 图片控件通过 `f505[f502=38].f503.f31.f6.f4` 引用该 ID；
- 因此「AI 生成素材 → 写入素材库 → 图片控件引用」的完整链路已闭合。

### 位置/尺寸 wire 路径（2026-08-22 第二轮差分闭合）

控件位置/尺寸存在载体组件 `f505[f502=12]` 的 transform 槽里（4 个槽，每个槽同构）：

```
f505[f502=12]              ← 载体（transform）
  f503.f13.f12.f501[n]     ← 4 个 transform 槽
    f502
      f501 = {f1:1, f2:1, f3:1}   ← 缩放 scale
      f502 = {f501:0.5, f502:0.5} ← 锚点 anchor
      f503 = {f501:0.5, f502:0.5} ← 中心 center
      f504 = {f501:X, f502:Y}     ← 位置（屏幕中心相对偏移，f32 fixed32）
      f505 = {f501:W, f502:H}     ← 尺寸
      f506 = {f501:0.5, f502:0.5} ← 锚点相关
      f508 = 空                    ← 旋转
```

**坐标换算**：wire 偏移 = 编辑器绝对坐标 − (800, 450)（1600×900 设计分辨率）。
实测 wire (−660, −250) ↔ 编辑器 (140, 200)。

> ⚠️ **编辑器 UI 坐标原点在左下角**（官方指南：「位置以画布左下角为原点，控件中心点相对位置」），
> x 向右、y 向上。因此 `(300, 200)` = 左下区域（不是左上）、`(1300, 700)` = 右上区域（不是右下）、
> `(800, 450)` = 正中心。**不要按左上角原点理解 editor 坐标**（2026-08-23 游戏核验实测：设 (300,200)
> 实际显示在左下、(1300,700) 显示在右上，与左下角原点语义一致）。

> 与 `assets:ui` 的 `setUiTransform` 实现一致（`gil_ui.ts` 已按此路径写 `f504` 位置 / `f505` 尺寸），
> 无需改生产代码。

### 未验证 / 待确认

- `f3={f501:-1}`、`f4=-1` 配置颜色后如何变（颜色覆盖的 wire）——需配置颜色后差分确认。
- 图片控件在游戏内是否真的显示该素材——需游戏核验。
- 图片源为「动态引用/道具/状态/技能/单位状态」时的 wire 差异——未验证。
- 移动位置时编辑器为何新增一条实例记录（1073741949 引用旧模板「图片引用」）——待归因。

## 节点图 UI 控制（2026-08-23 游戏核验闭合）

> 状态：已验证（真实注入 + 用户游戏核验）
> 来源：地图 1073741907 注入 `modifyUiControlStatusWithinTheInterfaceLayout` 节点，
> 把控件 1073741955（文本框）设为 Hidden，用户游戏核验「文本框隐藏」通过。

### 核心节点与参数

| 节点 | 作用 | 参数 |
| --- | --- | --- |
| `modifyUiControlStatusWithinTheInterfaceLayout` | 设置布局内控件(组)状态 | 目标玩家 / 界面控件索引 / 显示状态 |
| `switchCurrentInterfaceLayout` | 切换当前布局 | 目标玩家 / 布局索引 |
| `activateUiControlGroupInControlGroupLibrary` | 激活控件组库内控件组 | 目标玩家 / 界面控件组索引 |
| `getPlayerCurrentInterfaceLayout` | 查询当前布局 | 玩家实体 → 布局索引 |

### 关键规则（游戏核验锁定）

- **界面控件索引 = 控件完整 ID**（`0x40000000+` 段，如 1073741955），不是布局序号。
  DSL 里 `modifyUiControlStatus(player, 控件ID, status)` 直接传控件 ID（bigint）。
- **显示状态枚举**：`Off`（关闭：不可见+逻辑不运行）/ `On`（开启：可见+逻辑运行）/
  `Hidden`（隐藏：不可见+逻辑运行）。
- **玩家实体 = `player(1n)` 全局函数**（`dsl` 里直接调用，**不是 `f.player`**——`f.player`
  会报 `TypeError: f.player is not a function`；玩家序号从 1 开始）。
- **挂载**：图挂到普通实体（非关卡实体），`whenEntityIsCreated` 事件在实体创建时触发。

### DSL 最小示例

```ts
import { g } from 'genshin-ts/runtime/core'
import * as E from 'genshin-ts/definitions/enum'

const graph = g.server({ id: 1073741825 })
  .on('whenEntityIsCreated', (_evt, f) => {
    f.modifyUiControlStatusWithinTheInterfaceLayout(
      player(1n),          // 玩家 1 号实体
      1073741955n,         // 控件 ID（= 素材索引/控件 ID）
      E.UIControlGroupStatus.Hidden
    )
  })
```

编译产物 IR 确认：`modify_ui_control_status_within_the_interface_layout` 节点，
args = `[entity(玩家), int(控件ID), enum(hidden)]`。

## 待逐步还原

- 控件树、父子关系和布局坐标。
- 控件 ID 的定义、引用和关卡存储方式。
- 显示、隐藏、启用、禁用和关闭的精确差异。
- 按钮、文字等控件组件的配置结构（图片控件引用素材已闭合，见上）。
- 键盘和鼠标输入事件的参数。
- 按钮事件如何绑定到角色和节点图。
- 多人模式下 UI 状态与输入事件的隔离方式。
- UI 事件转发到其他实体的可靠模式。


## 素材体系与控件模板进阶（2026-08-27 学习资产实样闭合）

> 状态：框架 + 记录级结构已验证（真实 .gil 1073741911「学习资产」+ 源 .gia 双证据）；图元/页签项内部细节部分已闭合、部分待差分
> 来源：下载素材「好看的 UI(内含底板，牌类模版。包含底板和页签的使用实例).gia」导入地图 1073741911，wire 逐字节解析
> 证据层级：真实编辑器导出样本 + 真实地图回读；**未游戏验证**

### .gia 素材文件格式（编辑器导出，非节点图导出）

素材 .gia 的 Root 结构与节点图 .gia 不同框：

| Root 字段 | 内容 | 说明 |
| --- | --- | --- |
| f1（**重复出现**） | 素材容器 / 控件模板 / 节点图 / 复合定义 | 同一字段多次出现，protobuf 单数语义下**只有最后一个被保留**——当前 decode-gia.ts 只解出最后一条（which=1「获取GUID」），**丢掉了全部 UI 内容** |
| f2（重复） | 素材组记录 | which=15 |
| f3/f5 | filePath / gameVersion | 样本 filePath=285238552-1780388941-1073741861-UI文件.gia，version 6.6.0 |

GraphUnit 的 which 码（非 proto 枚举、新格式）：

| which | 含义 | payload 字段 | 样本内容 |
| --- | --- | --- | --- |
| 61 | 素材容器（底板/卡牌模版） | f19 | 底板1、红色卡牌模版（可换色）、仿官风格-底板、猫猫-底板等 13 条 |
| 21 | 控件模板（按钮） | f19 | 悬浮交互页按钮、唤出图片按钮、底板1-使用（使用实例） |
| 15 | 素材组（根级导出） | f19 | 图片/文本/花边/星星/页签/按钮状态素材组等 213 条 |
| 9 | 节点图 | f13 | 按钮打开悬浮交互页（7 节点）、按钮打开图片（12 节点）、获取玩家GUID（4 节点） |
| 1 | 复合定义 | f11（非已知 oneof 字段） | 获取GUID |

> 工具缺口：decode-gia.ts 与 gil-ui 工具链都不认识 which=61/21/15/1，
> 素材 .gia 需专用解析器（或先导入地图，再用 GIL 工具链读 root9）。

### 导入后 root9 记录类型码（type 码全集）

每条 502 记录（f501=记录 ID）内的 f502 子记录携带 type 码（f502[子].f502 = varint）：

| type | 含义 | 关键特征 |
| --- | --- | --- |
| 5 | 通用基础标记 | 所有记录都有：f11{f501=自身ID}, f501=1 |
| 6 | 素材组标记 | 上层 f12{f501=子类型}（188=文本框/189=图片/55=按钮组/13=悬浮页组等） |
| 7 | 布局 | 默认布局 1073741825 + 14 官方控件 |
| 4 | 控件模板 | f14 空槽；f501 目录登记 |
| 3 | 控件实例 | f13{f501=模板ID} 回指模板 |
| 13 | 悬浮页组标记 | 悬浮交互页按钮模板特有 |
| 55 | 素材容器 | f16 槽 + f503{f46{},f501=39,f502=55,f503=1,f504{}} |
| 5+4+55 并存 | 素材容器同时带模板标记 | listTemplates 与 listUiRecords 分类差异来源（root9 f501 目录=模板库，素材容器也登记） |

**root9 f501（packed varint）** = 模板库目录（39 条：素材容器+控件模板+布局+状态素材组），
assets:ui template list 按它筛选——不是误分类，是「模板库」语义。

### 素材容器（底板/卡牌模版）结构

素材容器 (type55, 无 f504)：
- f502[type5] 基础标记
- f502[type6] f12{f501=子类型}（分类副本标记）
- f502[type4] 模板标记（模板库登记）
- f502[type55] f503{...f4=自身ID} 素材容器自引用
- f503 = packed 子素材组 ID 列表
- 素材组子记录 (type6, f504=父容器ID)：f505[15] 名字 / f505[23] 分类副本回指 / f505[12] 图元数据 / f505[56]

样本：1073741920「枫丹风格-底板」= 素材容器 + 13 个素材组（文本框×1 + 图片×12）；
1073741851「红色卡牌模版（可换色）」= 素材容器 + 23 个素材组（文本/花边/白边/星星/饮料杯/分割线/渐变/底层）。

### 素材组图元数据（f505[12]）

f505[12].f503.f13.f12.f501[n]（n=0..3，4 个图元槽），每槽 f502（96B）：
- f501 {f1:1, f2:1, f3:1} 缩放 scale（f32×3）
- f502 {f501:0.5, f502:0.5} 锚点
- f503 {f501:0.5, f502:0.5} 中心
- f504 {f501:X, f502:Y} 位置（fixed32 f32）
- f505 {f501:W, f502:H} 尺寸
- f506 {f501:0.5, f502:0.5}
- f508 空 旋转

与图片控件 transform 槽同构（见上「位置/尺寸 wire 路径」）。素材组「图片」= 4 个图元；
颜色覆盖路径 f505.f503.f31.f4 = ARGB int32（assets:ui update --color 已实现）。

### 按钮控件模板 + 状态组件（t50）与变量组件（t41）

按钮模板（type4，样本 1073742048「悬浮交互页按钮」、1073742060「唤出图片按钮」）：
- 素材组子记录：悬浮交互页唤出/非悬浮交互页按钮/文本框等
- 按钮状态（t50 组件，2026-08-27 差分闭合）：f505[f502=50].f503.f43.f503 = 状态块序列，
  每块 f4 = 状态素材组 ID（0/缺省 = 无素材）；状态名 = [常态, 悬停, 按下, 状态4(候选选中/保留)]
- 对应独立素材容器：默认状态素材组/悬浮状态素材组/按下状态素材组/禁用状态素材组（type55 + 官方预制控件 图片子记录）
- 悬浮页形式变量（t41 组件，2026-08-27 差分闭合）：f505[f502=41].f503.f34 = 变量列表，
  每条 {f501?: 类型码(0=整数/1=浮点/2=字符串/3=动态文本域), f502: 变量名}，f503 = 序号

### 页签（tab）素材组子类型

页签素材组（样本 1073742008）除了通用 f505[15/23/12]，有专属子类型：

| subtype | 字段 | 推断 |
| --- | --- | --- |
| 46 | f39 槽 f502=f32 40.0, f505/f506=12B | 页签基座元素 |
| 47 | f40 槽含 6 条 ~180B f501 记录 | 页签项列表（每项 ~180B，内部待差分） |
| 48 | f41 槽 f507=50907 | 页签项状态配置 |
| 49 | f42 槽 f503=320006 | 页签项选择配置 |
| 58 | f47 槽 f48{f501=222B} | 页签容器数据 |
| 37 | f30 槽 f505=1, f507=1 | 通用组件槽 |

页签容器（1073742007，素材组）子记录：页签×2、游戏小tips:、交互页关闭按钮、图片。

### 悬浮交互页 wire（创建规范，CLI 已实现）

assets:ui create --type floating-page --id 模板ID 创建 4~6 条记录：
模板(t4) + 实例(t3) + 组 + 关闭按钮；无共享组时补模板侧组+关闭按钮模板（共 6 条）。
ID 派生：实例=模板-3、组=模板-2、关闭按钮=模板-1（冲突校验已有）。
DSL 运行时 API（definitions/nodes.ts 已生成）：show/closeFloatingInteractionPage、
updateFloatingInteractionPageListData、modifyUiControlStatusWithinTheInterfaceLayout、
switchCurrentInterfaceLayout、playUiAnimationOnControl；
事件 whenFloatingInteractionPageIsTriggered / whenUiControlGroupIsTriggered 已注册。
> 富版悬浮页（内容素材组/页签/形式变量）创建实现依据见 [ui-floating-page-rich-create.md](ui-floating-page-rich-create.md)。

### 使用实例：示例节点图 → DSL 等价（AI 生成节点图用）

| 示例图（1073741911 导入） | 节点构成 | DSL 等价 |
| --- | --- | --- |
| 按钮打开悬浮交互页（1073741825，7 节点） | whenUiControlGroupIsTriggered → Equal(Int=按钮ID) → showFloatingInteractionPage(Ety=self, Int=页ID, Dict{key: itemList}) | g.on('whenUiControlGroupIsTriggered', ...) + f.showFloatingInteractionPage(player(1n), pageId, {tabIndex: items}) |
| 按钮打开图片（1073741826，12 节点） | 两路 whenUiControlGroupIsTriggered → Equal(按钮ID) → Set UI Control (Group) Status On/Off（控件 1073743087/3088） | f.modifyUiControlStatusWithinTheInterfaceLayout(player(1n), controlId, On/Off) |
| 获取玩家GUID（1073741827，4 节点） | whenEnteringCollisionTrigger → queryGuidByEntity → setCustomVariable(玩家GUID) | 触发器 → GUID 查询 → 变量写回 |

按钮点击事件 payload（whenUiControlGroupIsTriggered）：uiControlGroupCompositeIndex + uiControlGroupIndex
两个 int；示例图用 Equal 比较其中一个（作者地图里的控件 ID 字面量，如 1073743061/1073743073）。

### 操作要点：导入后 ID 全部重分配

素材 .gia 导入地图时编辑器会重分配 ID（源 .gia 的 1073742011+ 段 → 目标地图 1073741843+ 段连续区间），
示例节点图里的控件 ID 字面量（1073743061 等）不会自动跟新——AI/CLI 配置 UI 时必须先
assets:ui list 读目标地图真实 ID，再写节点图字面量。这是「AI 直接配置 UI」的关键操作顺序。

### 状态块进阶：按钮 4 状态 = 默认/悬浮/按下/禁用（2026-08-27 学习资产实读闭合）

> 证据：1073741911「学习资产」悬浮交互页按钮（素材组 2049 t50）逐字节实读；CLI `assets:ui states` 已实现。
> 修正：之前"第 4 状态块（候选选中/保留）"悬而未决——**按钮第 4 块 = 禁用**。

- 按钮状态块（t50）：常态/悬停/按下/**禁用**，实读引用 2050(默认)/2052(悬浮)/2054(按下)/2056(禁用)
- **t50 状态容器双布局**：
  - A 打包式（自定义按钮 1853 型）：`f43.f503` = 4 块连接（块字段号 501..504）
  - B 展开式（关闭按钮 2021 型）：f43 顶层直接 4 块（f502 可能是 config 也可能是 block2——按块特征过滤：含 f503/f504 子对）
- 块结构统一：`{f501: {f2:1, f3:8, f4: 素材组ID}, f502: 空, f503: 尺寸对, f504: 缩放对, f505: 空}`

### 页签 4 状态 = 默认/悬浮/按下/选中（2026-08-27 实读闭合）

> 证据：1073741911 页签 2008 的 t58 组件。

- **页签状态组件 = t58**（不是 t50）：`t58.f503.f48.f501` = 4 块（同块结构）
- 引用 2009(默认)/2011(悬浮)/2014(按下)/**2017(选中)**
- **第 4 状态块语义由控件类型决定：按钮=禁用，页签=选中**

### CLI 状态列出命令（states）能力（2026-08-27）

```
gsts assets:ui states --id <控件ID>
# 支持：t50 打包式/展开式（按钮、关闭按钮）+ t58（页签）
# 输出：状态名 + 素材组引用（按钮第4块=禁用，页签第4块=选中）
```

### 「学习资产」地图 UI 构成全景（2026-08-27）

好看的 UI = 三层组合：

1. **素材资源库**（13 套）：枫丹风格-底板/猫猫-底板/身份卡/魔幻微童话-牌类/红色卡牌模版（可换色）/仿官风格-底板/纸本-牌类/蓝色卡牌模版/绿色-底板/底板1 等
   - 素材容器(type55) + 子素材组（文本框/图片，含图元数据 f505[12]）
2. **控件模板**（3 个）：悬浮交互页按钮(2048)/唤出图片按钮(2060)/底板1-使用(1989)
   - 骨架模板 + 子素材组（含 t50 状态）+ 文本框 + 状态素材组×4
3. **悬浮交互页**（1 个）：底板1-使用(1989)，t42 引用 7 个素材组（多页签容器），形式变量「页签1」
   - 页签容器 2007 → 页签×2 + 游戏小tips + 交互页关闭按钮 + 图片

4. 节点图（3 张）：按钮打开悬浮交互页/按钮打开图片/获取玩家 GUID（见上文 DSL 等价表）


## 节点图 UI 交互配置全链路（2026-08-27 学习资产示例图 + DSL 定义交叉验证）

> 状态：事件/动作 API 已从 definitions 与真实图交叉验证；挂载关系真实读取；**未游戏核验**
> 证据：1073741911「学习资产」3 张示例图 explain + definitions/events.ts|nodes.ts + assets:mounts list

### 交互事件（2 个，全部发到玩家节点图）

| 事件 | 触发源 | 出参 payload | DSL 注册 |
| --- | --- | --- | --- |
| whenUiControlGroupIsTriggered（界面控件组触发时） | 交互按钮/道具展示/自定义按钮/自定义开关 点击；只发给触发交互的玩家节点图 | eventSourceEntity/Guid, uiControlGroupCompositeIndex(组组合索引), uiControlGroupIndex(组索引=单控件组时即控件ID) | g.on('whenUiControlGroupIsTriggered') |
| whenFloatingInteractionPageIsTriggered（悬浮交互页操作触发时） | 页签/单选项视窗确认（须开【返回服务器事件】开关）+ 页内关闭按钮/交互按钮/道具展示/自定义按钮 | playerEntity/Guid, floatingInteractionPageIndex, interactiveItemIndex, listIndex[](列表索引列表), selectedListItem[](列表选中项列表，与 listIndex 一一对应) | g.on('whenFloatingInteractionPageIsTriggered') |

### 动作节点（11 个，服务端，目标玩家实体=player(1n)）

| 节点 | 参数 | 用途 |
| --- | --- | --- |
| activateUiControlGroupInControlGroupLibrary | 玩家, 控件组索引 | 激活控件组库内控件组 |
| removeInterfaceControlGroupFromControlGroupLibrary | 玩家, 控件组索引 | 移除已激活控件组 |
| switchCurrentInterfaceLayout | 玩家, 布局索引 | 切换当前布局 |
| modifyUiControlStatusWithinTheInterfaceLayout | 玩家, 控件ID, 状态(On/Off/Hidden) | 改控件显示状态（Off=不可见+逻辑停/On=可见+逻辑跑/Hidden=不可见+逻辑跑） |
| showFloatingInteractionPage | 玩家, 页索引, dict<页签索引,int_list> | 唤起悬浮页并初始化页签/单选项视窗数据 |
| closeFloatingInteractionPage | 玩家, 页索引 | 关闭悬浮页 |
| updateFloatingInteractionPageListData | 玩家, 列表索引, int_list, 是否默认选首项 | 更新页签/单选项视窗的显示列表项 |
| playUiAnimationOnControl | 玩家, 动效控件索引 | 播放界面动效控件 |
| refreshNotificationQueue | 玩家, 消息队列索引, 消息项ID | 更新消息队列 |
| getPlayerSCurrentUiLayout | 玩家 → int | 查询当前布局 |
| setWhetherPlayerSCursorClickPenetratesUiControls | 玩家, bool | 光标是否穿透 UI 控件 |

### 示例图交互模式（学习资产 3 张图）

1. **按钮 → 唤起悬浮交互页并初始化页签**（图 1073741825，7 节点）：
   When UI Control Group Is Triggered → Equal(事件Int, 按钮ID=1073743061) → Show Floating Interaction Page
   (Ety=Get Self Entity, Int=页ID=1073741898「底板1-使用」, Dict{页签索引: 列表项})
   → DSL：g.on('whenUiControlGroupIsTriggered') + f.showFloatingInteractionPage(player(1n), 1073741898n, {tabId: [...]})

2. **按钮 → 开关图片控件组**（图 1073741826，12 节点）：
   两路 When UI Control Group Is Triggered → Equal(按钮A=1073743073 / 按钮B=1073743088) →
   Set UI Control (Group) Status On/Off（控件 1073743087+1073743088）
   → DSL：f.modifyUiControlStatusWithinTheInterfaceLayout(player(1n), 控件ID, On|Off)

3. **获取玩家GUID 辅助**（图 1073741827，4 节点）：
   When Entering Collision Trigger → Query GUID by Entity → Set Custom Variable「玩家GUID」
   → 用途：UI 多人隔离时以玩家 GUID 作 key

### 挂载要求（交互事件到图的通路）

- UI 事件只发给**触发交互的玩家节点图**——事件图必须挂到玩家/角色实体或其 definition（assets:mounts attach），
  学习资产实测：按钮打开图片挂在 definition 1077936129（获取GUID 复合定义）。
- 图内所有控件 ID 均为**字面量**（按钮ID/页ID/控件ID），来自作者原地图；目标地图必须重映射（见上方「导入 ID 重分配」）。

### CLI 解析能力现状（2026-08-27 assets:ui 已支持）

- 列表：assets:ui list（素材/组/模板/实例/官方/布局分类）、template list、variables list（悬浮页 t41 形式变量）、states（t50 按钮 4 状态 / t58 页签 4 状态）
- 创建：create textbox|interactive-button|custom-button|image|floating-page（官方预制单条 + 布局注册）；template create/clone；library-inject（CSS 素材）
- 更新：update name/content/position/size/asset/color；delete
