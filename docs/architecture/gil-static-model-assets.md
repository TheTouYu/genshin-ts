# 静态 `.gil` 模型元件与装饰物结构

> 状态：待验证
> 来源：真实地图 `.gil` wire 扫描 + 候选自动检查 + 写回实验 + 用户游戏反馈
> 最近校验：2026-07-25
> 适用范围：当前地图的静态模型实验；各结论按章节证据等级区分，不等同于通用 GIL 协议

本文档记录在地图 `.gil` 中研究静态模型资产、基础元件、自定义元件和装饰物的结构观察。它是持续维护文档：后续每获得一个新的真实地图样本，应优先补充“样本登记”和“A/B 差分记录”，再更新结构结论。

## 1. 证据边界

必须区分以下证据层级：

- **真实地图观察**：来自实际 `.gil` 文件的字段路径、实例 ID、资源 ID、wire 类型和 fixed32 字节。
- **用户编辑器操作**：用户说明某个对象基于哪个元件、修改了哪些属性；这是操作来源，不自动证明字段语义。
- **自动解析**：脚本对 protobuf-like length field 的结构化扫描，只证明文件中存在对应字节结构。
- **写回/注入**：本研究截至当前没有执行，不能据此判断游戏是否接受复制后的资产。
- **游戏内验证**：截至当前没有针对静态模型 `.gil` 写回方案完成验证。

protobuf 默认值不能替代字段存在性。文档中“缺失”表示原始 wire 中没有该字段，不表示其语义值一定不是零。

## 2. 当前样本登记

### 2.1 样本 A：当前地图

| 项目 | 值 |
|---|---|
| 地图 | `mapId = 1073741847` |
| 文件 | `<Beyond_Local_Export>/.../Beyond_Local_Save_Level/1073741847.gil` |
| 文件大小 | 41927 bytes |
| SHA-256 | `f273ed0e7093676f88c3c9215aa9c9970d01c163e3353219eda3827ca6b90d07` |
| 采集方式 | 编辑器保存后，对地图文件只读扫描 |
| 用户操作 | 新增基础元件“球体”；创建“新测试球体”；创建“新测试球体2”，并添加装饰物列表中的“球体” |
| 写回状态 | 未写回、未注入 |
| 游戏验证 | 未验证 |

本样本使用的主要命令：

```bash
GSTS_LOCALLOW_DIR=<已确认的 LocalLow 目录> \
  node bin/gsts.mjs maps -c gsts.config.ts

npx tsx tools/inspect-gil-custom-variables.ts <map.gil> 新测试球体
npx tsx tools/inspect-gil-custom-variables.ts <map.gil> 新测试球体2
npx tsx tools/inspect-gil-custom-variables.ts <map.gil> 球体 5
```

此外使用了只读的原始 wire 字段扫描，定位 `8.1` 下的实例容器并递归显示 field、wire、偏移、fixed32 浮点和嵌套消息。

## 3. 用户操作与文件中可观察对象

当前研究的比较主线已按用户确认固定为逐层复杂化：

```text
基础元件：球体
    ↓ 基于球体创建
自定义元件：新测试球体
    ↓ 基于新测试球体创建，并增加装饰物列表
自定义元件：新测试球体2
```

这三层不能只按场景实例的 Transform 比较，必须分别比较：元件定义记录、元件 ID、基础资源/父元件引用、场景实例引用、装饰物列表和装饰物子实例。

用户提供的编辑器操作为：

1. 新增基础元件：`球体`。
2. 新增自定义元件：`新测试球体`。
   - 基于基础元件 `球体`。
   - 修改了旋转、缩放等参数。
3. 新增自定义元件：`新测试球体2`。
   - 基于自定义元件 `新测试球体`。
   - 增加装饰物列表 `球体`。
   - 装饰物使用基础元件 `球体`。
   - 设置装饰物的缩放、相对自身的位置偏移和旋转。

当前 `.gil` 中可定位的名称出现次数：

| 名称 | UTF-8 命中数 | 可识别的实例/定义用途 |
|---|---:|---|
| `球体` | 5 | 其中第 5 次是 `8.1.5.11.1` 下的装饰物实例名称；其余命中属于嵌套或其它元数据上下文 |
| `新测试球体` | 4 | 定义/实例相关名称 |
| `新测试球体2` | 3 | 定义/实例相关名称 |
| `装饰物列表` | 0 | 当前样本没有该中文字符串的 UTF-8 直接命中 |

“装饰物列表”没有直接字符串命中，只能说明该 UI 名称没有按当前扫描方式作为 UTF-8 字段保存；不能说明列表不存在。

## 4. 顶层分区：`field 4` 与 `field 8`

在当前样本 payload 顶层：

```text
field 4：长度 4583 bytes
field 8：长度 3045 bytes
```

当前观察显示：

- `field 4` 内有一组元件/Prefab 定义样式的记录。
- `field 8` 内有地图中实际摆放的实例记录。
- 两个区域可能复用相同的数字 ID，但数字 ID 不能脱离父容器和字段路径解释。
- 因此不能只在整个文件中搜索一个 ID，就断言它全局只有一种语义。

`field 8` 的路径为：

```text
8.1
```

该容器当前有 5 个重复的 `field 1` 实例消息，长度分别为：

```text
765, 561, 571, 580, 553 bytes
```

## 5. `field 8` 实例清单

| 顺序 | 实例路径 | 实例 ID | 长度 | 名称/用途（当前观察） |
|---:|---|---:|---:|---|
| 1 | `8.1.1` | `1077936129` | 765 | 其它元件，含名称 `变量专用` |
| 2 | `8.1.2` | `1077936131` | 561 | `长方体` |
| 3 | `8.1.3` | `1077936132` | 571 | `新测试球体` |
| 4 | `8.1.4` | `1077936133` | 580 | `新测试球体2` |
| 5 | `8.1.5` | `1077936134` | 553 | 名称为 `球体`，与装饰物操作对应 |

注意：`8.1.2` 的实例 ID `1077936131` 与 `field 4` 中的某个定义记录数字相同。当前只能按“路径 + 父容器 + 子字段”区分，不能把所有 ID 当作一个简单的全局命名空间。

## 6. 通用实例字段形状

当前几个模型相关实例都呈现类似的外层结构：

```text
field 1：实例 ID
field 2：资源、定义或父实例关系
field 5：实例元数据列表
field 6：属性列表，其中 field 1 = 1 的条目包含 Transform
field 7：关联/附加记录列表
field 8：资源或 Prefab 标记
```

这些是当前样本中的字段编号观察，不是已经确认的官方 protobuf schema 名称。

### 6.1 名称

实例名称常见路径：

```text
field 5
└── field 1 = 1
    └── field 11
        └── field 1 = <名称>
```

例如：

```text
8.1.3.5.11.1 = `新测试球体`
8.1.4.5.11.1 = `新测试球体2`
8.1.5.5.11.1 = `球体`
```

### 6.2 Transform

当前观察到的 Transform 路径：

```text
8.1.<实例序号>.6
└── field 1 = 1
    └── field 11
        ├── field 1：位置
        ├── field 2：旋转
        └── field 3：缩放
```

当前样本中这三个子字段使用 length-delimited 外层消息，位置和缩放内部使用 fixed32 little-endian 浮点。

### 6.3 其它稳定出现的元数据

多个模型实例都包含以下样式字段：

- `field 5.field 1 = 13`，内部出现 `field 22.field 4 = 4294967295`。
- `field 5.field 1 = 14`，内部出现 `MPActionGroup`。
- `field 5.field 1 = 38`，内部 `field 48.field 1` 为 fixed32 `1.0`。
- `field 5.field 1 = 111`、`61`、`62` 等空消息条目。
- `field 6.field 1 = 11`，内部包含 `GI_RootNode`、`中心原点`、`RootNode`。
- `field 7.field 1 = 18`，内部包含 `受击特效`、`被击倒特效` 和 `GI_RootNode`。

这些条目目前只能作为复制时需要保留的结构记录；不能仅凭字段号给出正式语义名称。

## 7. 基础元件“球体”

### 7.1 资源与场景实例

当前可确认的基础球体资源 ID：

```text
10009002
```

与用户新增装饰球体对应的场景实例：

```text
路径：8.1.5
实例 ID：1077936134
名称：球体
长度：553 bytes
```

其资源相关结构为：

```text
field 2:
  field 1 = 10009002
  field 2 = 1

field 8 = 10009002
```

`field 2.field 2 = 1` 的正式含义待验证；不能直接命名为“装饰物标志”。

### 7.2 Transform wire 观察

```text
8.1.5.6
└── field 1 = 1
    └── field 11
        ├── field 1：位置
        │   ├── field 1 = 4.085911，wire fixed32，bytes `c8bf8240`
        │   └── field 3 = -9.616045，wire fixed32，bytes `52db19c1`
        ├── field 2：空消息
        └── field 3：
            ├── field 1 = 1.0，bytes `0000803f`
            ├── field 2 = 1.0，bytes `0000803f`
            └── field 3 = 1.0，bytes `0000803f`
```

位置的 y 分量在原始 wire 中未出现。当前不能把它记录成“明确为 0”；只能记录为“field 2 缺失”。旋转也是空消息，而不是三个显式的零 fixed32。

## 8. 自定义元件“新测试球体”

### 8.1 定义/资源关系观察

在 `field 4` 中观察到与名称 `新测试球体` 相关的定义记录，记录 ID 为：

```text
1077936130
```

该记录中观察到：

```text
field 2 = 10009002
```

场景实例位于：

```text
路径：8.1.3
实例 ID：1077936132
名称：新测试球体
长度：571 bytes
```

实例的关系字段：

```text
field 2:
  field 1 = 1077936130

field 8 = 10009002
```

当前最保守的结构表达为：

```text
场景实例 1077936132
└── field 2.field 1 → 记录 1077936130
    └── field 2 → 资源 10009002
```

“记录 1077936130”可以称为定义/来源样式记录；其正式 schema 语义仍待更多样本确认。

### 8.2 Transform

```text
位置：[5.70841, 1.270447, -6.333962]
旋转：field 2.field 1 = 90
缩放：[2.0, 2.0, 2.0]
```

对应 fixed32 bytes：

```text
位置 x：`4babb640`
位置 y：`009ea23f`
位置 z：`d2afcac0`
旋转值：`0000b442`
缩放：`00000040`、`00000040`、`00000040`
```

旋转当前只观察到一个分量 field 1，不能把它无证据扩展为完整欧拉角或四元数语义。

## 9. 自定义元件“新测试球体2”

### 9.1 场景实例与来源引用

场景实例：

```text
路径：8.1.4
实例 ID：1077936133
名称：新测试球体2
长度：580 bytes
```

直接关系字段：

```text
field 2:
  field 1 = 1077936131
```

同时存在另一处同值引用：

```text
field 5:
  field 1 = 40
  field 50:
    field 501 = 1077936131
```

因此当前样本确认：同一个关系 ID `1077936131` 至少在两个不同字段路径中出现：

```text
8.1.4.2.1 = 1077936131
8.1.4.5（field 1 = 40）.50.501 = 1077936131
```

复制或生成嵌套自定义元件时，不能只替换实例 ID 后假定引用关系自动成立；至少需要扫描并处理这两类引用。

### 9.2 Transform

```text
位置：[10.20347, 1.559082, -9.350883]
旋转：field 2.field 1 = 90
缩放：[2.0, 2.0, 2.0]
```

对应 fixed32 bytes：

```text
位置 x：`68412341`
位置 y：`0090c73f`
位置 z：`379d15c1`
旋转值：`0000b442`
缩放：`00000040`、`00000040`、`00000040`
```

## 10. 装饰物“球体”

当前装饰物不是父实例中的一段纯坐标字节，而是一个单独的 `field 8.1` 实例：

```text
新测试球体2
└── 相关的独立场景实例 1077936134
    ├── 名称：球体
    ├── field 2.field 1 = 10009002
    ├── field 2.field 2 = 1
    ├── field 8 = 10009002
    └── 自己的 field 6 Transform
```

当前样本没有发现字符串 `装饰物列表`，但发现了装饰物实例本身。因此目前可确认“有独立球体实例”，不能确认列表标题的具体编码方式。

## 11. 当前结构图

以下结构只表达当前样本已观察到的引用，不把待验证字段提升为正式 schema：

```text
field 4：定义/资源样式记录
│
├── 记录 1077936130
│   ├── 名称：新测试球体
│   └── field 2 = 10009002
│
└── 其它定义/资源样式记录

field 8.1：地图实例列表
│
├── 实例 1077936131
│   └── 名称：长方体
│
├── 实例 1077936132
│   ├── 名称：新测试球体
│   ├── field 2.field 1 → 1077936130
│   ├── field 8 = 10009002
│   └── Transform：[5.70841, 1.270447, -6.333962] / 90 / [2,2,2]
│
├── 实例 1077936133
│   ├── 名称：新测试球体2
│   ├── field 2.field 1 → 1077936131
│   ├── field 5(field 1=40).field 50.field 501 = 1077936131
│   └── Transform：[10.20347, 1.559082, -9.350883] / 90 / [2,2,2]
│
└── 实例 1077936134
    ├── 名称：球体
    ├── field 2.field 1 = 10009002
    ├── field 2.field 2 = 1
    ├── field 8 = 10009002
    └── Transform：[4.085911, 缺失, -9.616045] / 空旋转 / [1,1,1]
```

## 12. 已确认、待验证与禁止假设

### 已确认的样本事实

- 当前样本 `8.1` 下有 5 个独立实例容器。
- 实例容器具有独立的实例 ID、元数据、属性和 Transform。
- `新测试球体`、`新测试球体2`、装饰物 `球体` 都能在实例记录中定位。
- 自定义元件相关实例存在对其它记录/实例 ID 的引用。
- `新测试球体2` 中的 `1077936131` 至少出现在 `field 2.field 1` 和 `field 5.field 50.field 501` 两个路径。
- Transform 使用稀疏的 length-delimited 子消息和 fixed32 浮点；零旋转可以编码为空消息。
- 装饰物球体有独立实例和独立 Transform。

### 待验证

- `field 4` 中各记录的正式容器语义，以及定义 ID 与场景实例 ID 的分配规则。
- `field 2.field 2 = 1` 的正式含义。
- `field 5.field 1 = 40` / `field 50.field 501` 是否专门表示自定义元件来源、嵌套层级或装饰物关系。
- `field 7`、`field 8` 是否存在必须同步更新的反向索引。
- 装饰物位置是否是父级局部坐标、地图坐标，或另一种编辑器坐标。
- 删除、复制多个相同装饰物后，列表记录和实例顺序的变化。
- 实例 ID 是否在地图范围内单调递增、是否存在回收或跨区域唯一约束。
- 同一资源 ID 是否跨地图稳定可用。
- `.gil` 写回后编辑器和游戏的接受规则。

### 元件 ID 分配规则（用户确认）

用户确认当前地图的自定义元件 ID 采用连续递增规则：

```text
新测试球体   = 1077936130
新测试球体2  = 1077936131
后续新自定义元件继续递增：1077936132、1077936133、...
```

这组规则来自用户对参考地图的确认，当前文档将其作为该地图/该编辑器样本的操作规则记录。它不能自动推广为所有地图、所有版本或所有 ID 命名空间的通用协议；后续写回仍需检查是否与现有定义 ID 冲突。

必须区分：

- **元件 ID**：用户确认的自定义元件连续 ID，例如 `1077936130`、`1077936131`。
- **场景实例 ID**：`field 8.1` 中实际摆放对象的实例 ID，例如参考样本中的 `1077936132`、`1077936133`。
- 两类 ID 在当前样本中数值相邻，不能只凭数值大小判断其语义；必须结合 `field 4`/`field 8` 和引用路径判断。

### 禁止提前假设

- 不要把 `createPrefab()` 运行时能力当作静态 `.gil` 编码证据。
- 不要只复制一个实例容器并替换一个 ID。
- 不要把 `10009002` 当作所有地图都稳定可用的全局球体资源 ID。
- 不要把名称字段当作资源 ID，也不要把 UI 名称 `装饰物列表` 当作文件字段名。
- 不要把缺失的 fixed32 分量自动补写为显式零。
- 不要把自动解析、文件成功写回或注入成功表述为游戏内验证。

## 13. 后续样本与 A/B 差分模板

每次新增样本时记录：

```md
### 样本 <编号>

- 来源操作：
- 地图 ID：
- 文件大小：
- SHA-256：
- 保存时间：
- 是否与上一样本保留同一地图状态：
- 只改变的因素：
- 游戏/编辑器观察：
- 游戏内验证：未验证 / 用户确认结果

#### 相关实例

| 名称 | 路径 | 实例 ID | 容器长度 | 资源/来源字段 |
|---|---|---:|---:|---|
|  |  |  |  |  |

#### Transform wire

| 实例 | 位置字段 presence | 位置 bytes | 旋转字段 presence | 旋转 bytes | 缩放 bytes |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

#### 引用差分

- 新增 ID：
- 删除 ID：
- ID 替换位置：
- `field 2`：
- `field 5`：
- `field 7`：
- `field 8`：
- 其它疑似索引：
```

优先采集的对照样本：

1. 只移动装饰物位置。
2. 只改变装饰物旋转。
3. 只改变装饰物缩放。
4. 在同一列表增加第二个相同球体。
5. 删除装饰物后保存。
6. 复制 `新测试球体2`，只改变副本的名称或 Transform。

每个 A/B 样本都要同时保存原文件、文件大小、SHA-256、名称命中、实例容器路径、字段 presence 和原始 fixed32 bytes。不要只比较解码后的默认值 JSON。

## 14. 追加式实验记录（2026-07-24）

本次实验不是 `.gia` NodeGraph 注入，而是对真实地图 `.gil` 的直接追加写回。目标地图和原文件均已保留备份；没有修改 `field 4` 定义区、NodeGraph 或原有实例。

### 写回前后

| 项目 | 写回前 | 写回后 |
|---|---:|---:|
| 文件大小 | 41927 bytes | 45850 bytes |
| SHA-256 | `f273ed0e7093676f88c3c9215aa9c9970d01c163e3353219eda3827ca6b90d07` | `3acf83a27751005327a8b86ea864805af4d26b71ff12201ee59ec6cf24632022` |
| `8.1` 实例数 | 5 | 12 |
| `field 4` 长度 | 4583 bytes | 4583 bytes |
| NodeGraph 列表 | 未改变 | 未改变（只读复核） |

备份文件名：

```text
1073741847.gil.static-model-exp-20260724-131409.bak
```

### 新增测试点

| 测试点 | 来源实例 | 新实例 ID | 新 Transform |
|---|---:|---:|---|
| `sphere-default` | 1077936134 | 1077936135 | 位置 `[0,0,0]`，空旋转，缩放 `[1,1,1]` |
| `sphere-position` | 1077936134 | 1077936136 | 位置 `[3,0,0]`，空旋转，缩放 `[1,1,1]` |
| `sphere-rotation` | 1077936134 | 1077936137 | 位置 `[6,0,0]`，旋转 `45`，缩放 `[1,1,1]` |
| `sphere-scale` | 1077936134 | 1077936138 | 位置 `[9,0,0]`，空旋转，缩放 `[2,2,2]` |
| `custom-sphere` | 1077936132 | 1077936139 | 位置 `[0,3,0]`，旋转 `180`，缩放 `[0.5,0.5,0.5]` |
| `nested-custom` | 1077936133 | 1077936140 | 位置 `[0,6,0]`，旋转 `270`，缩放 `[1.5,1.5,1.5]` |
| `decorative-sphere` | 1077936134 | 1077936141 | 位置 `[0,9,0]`，旋转 `30`，缩放 `[0.75,0.75,0.75]` |

### 结果边界

- 写回脚本只复制实例消息，替换顶层实例 ID，并替换实例自身 `field 6` 中 `field 1 = 1` 的 Transform。
- 原始未知字段、元数据和引用内容在副本中保留；本轮没有为副本重新建立新的父子/反向索引。
- 自动复核证明文件头尾有效、实例数量增加、定义区长度未变化、NodeGraph 列表未变化。
- 自动复核不证明编辑器或游戏会显示这些对象。

### 用户游戏/编辑器反馈

用户反馈：

1. 基础“球体”元件的创建相关功能正常。
2. 没有看到基于“球体”创建的“新测试球体”相关测试对象。
3. “新测试球体2”创建成功，但其装饰物列表为空。

结合写回后的只读扫描，当前结论为：

- **基础球体路径：部分验证成立。** 直接复制 `1077936134` 并只修改实例自身 Transform，可以让基础球体测试点进入地图处理路径；具体显示结果由用户确认。
- **自定义元件路径：验证失败。** `1077936139` 和 `1077936140` 只是 `field 8.1` 中的实例消息副本。上一轮没有复制或重建 `field 4` 中的定义/资源记录，也没有确认自定义元件定义与场景实例之间的完整索引，因此不能称为创建了新的自定义元件。
- **装饰物路径：验证失败。** `1077936140` 的副本虽然保留了来源引用样式字段，但没有建立独立的装饰物列表容器或父子索引；用户观察到“装饰物列表为空”说明仅复制父实例不能恢复装饰物列表。
- 当前文件扫描仍显示 `field 4` 长度为 4583 bytes，`装饰物列表` UTF-8 命中数为 0；新增名称只出现在追加实例中。这进一步支持“定义区/列表关系未复制”的判断。

因此后续实现必须先解决：

1. 自定义元件定义记录的创建或复用规则；
2. 定义记录与地图实例的完整引用关系；
3. 装饰物列表的真实容器、子实例记录和父子/反向索引；
4. 自定义元件与装饰物的多实例 ID 分配和关联规则。

在这些结构未通过新的真实 A/B 样本确认前，不再追加自定义元件或装饰物到地图。

## 15. 失败实验后的当前结论（2026-07-24）

用户对“新测试球体4”的游戏反馈：

- 新元件生成成功；
- 元件 ID 正常；
- 装饰物丢失。

这说明当前复制逻辑已经覆盖了以下层级：

```text
元件定义记录 → 新元件 ID → 场景实例 → 主实例 Transform
```

但没有恢复装饰物的运行时可见关系。自动 wire 扫描虽然观察到“新测试球体4”定义中存在两个递进层级记录：

```text
1077936132（新测试球体3）
1077936133（新测试球体4）
```

且新场景实例 `1077936136` 的 `field 2.field 1 = 1077936133`，但这不足以证明装饰物列表已绑定到新组件。当前失败表明至少还缺少以下一种或多种关系：

- 装饰物列表的专用索引/容器；
- 装饰物定义到新父元件 ID 的反向引用；
- 定义区和场景区之间的装饰物所有者关系；
- `field 27` 或其它顶层索引中的新元件登记；
- 装饰物实例 ID 与父组件定义 ID 的映射，而不是简单保留原始嵌套记录。

因此“自动扫描存在嵌套记录”不能再作为“装饰物结构复制成功”的验证标准。新的验收标准必须包含用户游戏中确实看到装饰物，并且至少对照 `新测试球体2 → 新测试球体3 → 新测试球体4` 的所有新增 ID 和所有引用位置。

当前不再继续写回。下一步优先需要对 `新测试球体2`、`新测试球体3` 和新生成的 `新测试球体4` 做完整的跨顶层字段引用反查，尤其是扫描所有字段中指向装饰物资源 `10009001/10009002`、父元件 ID 和实例 ID 的位置。

## 16. `新测试球体2` / `新测试球体3` 差分结论（2026-07-24）

用户反馈：基于真实“新测试球体3”复制出的“新测试球体4”元件和 ID 正常，但装饰物丢失。对参考样本进行 `1077936131 → 1077936132`、`1077936133 → 1077936135` 的 raw wire 对比后，当前最重要的差异位于 `field 5.field 1 = 40.field 50.field 501`。

### 16.1 定义记录的差异

```text
新测试球体2 定义 1077936131：
field 6:
  field 1 = 40
  field 50:
    field 501 packed IDs = [1077936130]

新测试球体3 定义 1077936132：
field 6:
  field 1 = 40
  field 50:
    field 501 packed IDs = [1077936134, 1077936135]
```

对应 raw 片段：

```text
新测试球体2：`aa1f058280808004`
新测试球体3：`aa1f0a86808080048780808004`
```

其中 varint 解码值为：

```text
1077936130 = `8280808004`
1077936134 = `8680808004`
1077936135 = `8780808004`
```

这不是简单的单个父元件 ID 替换；真实“新测试球体3”把 `field 50.field 501` 扩展成了两个重复 ID，说明新增长方体后，装饰物/子项关系以重复引用列表形式增长。

### 16.2 场景实例的差异

```text
新测试球体2 实例 1077936133：
field 2.field 1 = 1077936131
field 5(field 1=40).field 50.field 501 packed IDs = [1077936131]

新测试球体3 实例 1077936135：
field 2.field 1 = 1077936132
field 5(field 1=40).field 50.field 501 packed IDs = [1077936136, 1077936137]
```

对应 raw 片段：

```text
新测试球体2实例：`aa1f058380808004`
新测试球体3实例：`aa1f0a88808080048980808004`
```

因此 `field 50.field 501` 在定义记录和场景实例中都参与关系表达，但两者的值并不是同一类简单的“父元件 ID”：定义记录中的列表与场景实例中的列表呈现不同的 ID 集合。

### 16.3 对此前“装饰物丢失原因”的修正

此前根据单个字段的差异，曾推断新组件的 `field 40.field 501` 为空就意味着装饰物列表没有复制。这一推断已被用户的游戏/编辑器反馈否定。

用户确认当前真实编辑器创建的“新测试球体4”全部正常：

- 元件生成成功；
- 元件 ID 正常；
- 组件显示正常；
- 装饰物列表正常；
- 原有球体和新增长方体装饰物均保留。

因此当前必须修正为：

```text
field 40.field 501 为空
≠ 装饰物列表为空
```

装饰物可能通过父组件继承、`field 27` 登记、其它顶层索引或多个关联记录共同解析。不能只看一个字段的 presence 就给出“装饰物丢失”的结论。

上一轮由脚本复制出的“新测试球体4”装饰物丢失，原因仍待精确定位。可以确认的是：脚本复制结果与编辑器真实生成结果存在结构差异；但在当前证据下，不能把差异单独归因于 `field 40.field 501`，也不能把 `field 27` 单独认定为决定性索引。

### 16.4 当前正确的对照方向

当前最有价值的成功对照是：

```text
编辑器真实生成的 新测试球体3 → 新测试球体4
```

应以该成功样本与脚本失败样本逐字段、逐 wire 对比，尤其检查：

1. `field 4` 定义记录的完整字节顺序和所有未知字段；
2. `field 8` 场景实例的完整字节顺序；
3. `field 27` 新增记录及其与元件/实例 ID 的关系；
4. `field 40.field 501` 的实际作用，而不是仅凭空/非空判断；
5. 定义记录名称与场景实例名称是否应分别处理；
6. 编辑器是否在保存时重写或补充了父子关系；
7. 脚本失败样本中是否错误保留了旧实例 ID或旧引用。

当前结论：用户游戏验证证明真实编辑器生成的“新测试球体4”结构可用；脚本复制装饰物失败的根因仍为待验证。下一步应围绕成功编辑器样本做差分，不应继续基于单字段猜测写回。

## 17. 深入比对：辅助记录与装饰物所有者关系（2026-07-24）

本节基于配置解析出的真实地图目录和只读 raw wire 扫描，补充正常“新测试球体4”与脚本失败“新测试球体5”的差异。样本路径为：

```text
/mnt/c/Users/touyu/AppData/LocalLow/miHoYo/原神/BeyondLocal/110170759/Beyond_Local_Save_Level/1073741847.gil
```

正常样本的元件/实例 ID 为 `1077936135`；失败样本新增元件/实例 ID 为 `1077936136`。

### 17.1 正常样本的辅助记录闭包

正常样本中，`1077936135` 的定义和实例均存在装饰物关系列表，但列表中的 ID 不是资源 ID，而是两组辅助记录 ID：

```text
field 4 / 定义 1077936135：
  field 6.field 50.field 501 = [1073741842, 1073741843]

field 8 / 实例 1077936135：
  field 5.field 50.field 501 = [1073741844, 1073741845]
```

这些 ID 在顶层 `field 27` 分区有对应记录：

```text
field 27：1073741842、1073741843、1073741844、1073741845
```

本轮进一步核对发现，`field 9` 没有随“新测试球体4”新增 `1073741844/1845` 记录；因此目前只能把 `field 27` 作为已证实发生变化的登记区，不能预设 `field 9` 是必需新增区。

`field 27` 记录中观察到：

```text
1073741842：resource 10009002，名称样式“装饰物_1”，关系字段指向 1077936135
1073741843：resource 10009001，名称样式“装饰物_2”，关系字段指向 1077936135
```

`field 27` 的 `field 2` 记录使用另一组编号（`1073741844`、`1073741845`），并通过其内部关系字段回指定义侧记录。这里仅确认 wire 结构，不将 field 9/27 命名提升为正式 schema 语义。

### 17.2 失败样本缺失的闭包

失败样本虽然新增了 `field 4` 定义和 `field 8` 实例 `1077936136`，但它们仍然引用正常组件的辅助记录：

```text
定义 1077936136：field 6.field 50.field 501 = [1073741842, 1073741843]
实例 1077936136：field 5.field 50.field 501 = [1073741844, 1073741845]
```

同时：

```text
field 27：没有发现新的 1077936136 owner 记录
field 9：与正常样本相比没有新增记录
```

因此脚本结果不是完整的自定义元件复制，而是“新主记录共享旧 `field 27` 装饰物登记闭包”。这比“只缺少 `field 40.field 501`”更能解释用户观察到的主元件存在、装饰物列表为空。

### 17.3 当前结论和待验证项

当前真实地图扫描支持的最强结论：

```text
自定义元件的可见装饰物关系具有跨顶层记录闭包：
主定义/实例、field 27 登记记录、
以及定义侧/实例侧的 field 50.field 501 引用必须共同一致。

`field 9` 当前是排除项：本组样本尚未观察到它为该复制操作新增记录。
```

尚不能仅凭这一组样本确定：

- `field 9` 与静态元件装饰物复制是否存在间接关系；
- `field 27` 是资源登记、owner 反向索引还是编辑器缓存；
- 哪一组缺失会单独导致装饰物不显示；
- 新辅助记录 ID 是否必须连续，还是只需不冲突。

后续应优先编写只读结构闭包检查器，输入真实样本，输出每个自定义元件的定义侧 ID 列表、实例侧 ID 列表、`field 9/27` 记录和 owner 反查结果。在闭包检查器通过多个真实编辑器样本前，不应写回新元件。

## 18. 完整辅助记录闭包写回实验（2026-07-25）

> 状态：已验证
> 来源：真实地图 wire 扫描 + 候选自动闭包断言 + 地图写回 + 用户游戏内验证
> 适用范围：地图 `1073741847.gil` 的“新测试球体5”候选；不推广为通用 GIL schema

本轮在用户明确确认后，以当前地图中用户此前确认正常的“新测试球体4”为模板，首次把主定义、
主实例和 `field 27` 两侧辅助记录作为一个闭包共同复制。写回前当前地图 SHA-256 为
`9f101c66f021e7cac1493f618f433945c04b267c3286fc7c3aa0ee8312465194`；它与正常基线大小相同，
只在顶层 `field 4`、`field 5`、`field 46` 有保存差异，`field 8/9/27` 与正常基线一致。因此本轮保留
当前地图状态作为来源，没有静默恢复旧基线。

实验映射为：

```text
主定义/实例：1077936135 → 1077936136
定义侧辅助：1073741842/1843 → 1073741846/1847
实例侧辅助：1073741844/1845 → 1073741848/1849
资源：10009002（球体）、10009001（长方体）
名称：新测试球体5
位置：[50, 0, 0]
```

候选自动断言确认：只有顶层 `field 4/8/27` 发生变化；`field 9` 原始字节保持不变；旧辅助 ID 的
全文件出现次数没有增加；新辅助 ID 形成独立的定义侧/实例侧记录和 owner/反向引用；NodeGraph
列表及节点数量摘要与写回前相同。自动断言只证明本轮候选闭包和文件结构，不证明这些未知字段的
正式语义或游戏显示。

写回结果：

```text
目标：Beyond_Local_Save_Level/1073741847.gil
写回后大小：46210 bytes
写回后 SHA-256：94ad9b665936935e8d35fa558db4617b06d854d228255e6e6873067593b7418f
保护备份：1073741847.gil.before-static-model-closure-20260725-111720.bak
保护备份 SHA-256：9f101c66f021e7cac1493f618f433945c04b267c3286fc7c3aa0ee8312465194
```

写回后已重新运行 raw 扫描和 NodeGraph 列表扫描，确认文件可读且新闭包存在。用户随后在游戏中
确认“新测试球体5”的装饰物列表正常、ID 正常、名称正常，并明确同意标记通过。因此本候选的证据
等级已提升为“写回成功 + 自动结构扫描成功 + 用户游戏内验证通过”。该结论证明本轮完整闭包候选
可被游戏接受。用户测试/保存后，目标地图变为 `46459 bytes`、SHA-256
`e768d5cb4237b497a0c4c12a6d4690c6c5331db821ebbe3d7c2fab38abc79335`；只读复扫确认主 ID、四个辅助
ID 和 `field 27` 闭包仍存在，并观察到编辑器在 `field 6` 增加了指向 `1077936136` 的登记。该测试后
哈希是外部保存状态，未被脚本覆盖。结论只覆盖上述地图状态、组件形态和候选文件；不能据此断言
`field 27` 的正式 schema 语义，也不能自动推广到无装饰物、单装饰物、更多装饰物或其它地图。

## 19. H1 失败的只读数量差分诊断（2026-07-25）

用户进一步确认：H1 在**元件列表和场景中都没有**，不是仅装饰物列表为空。对测试2（1 件装饰物）、
测试3/4/5（均为 2 件且真实成功）和 H1（7 件、失败）运行一次性只读 wire 检查后，发现旧自动断言
遗漏了三类差异。

第一，H1 主定义的名称 wire 已损坏。生成器把 UTF-8 名称从 16 bytes 的“新测试球体5”裸替换为
14 bytes 的“静态拼装H1”，但没有重写内层 length-delimited 长度：

```text
真实测试5：`0a 10 <16-byte name> 32 0b ...`
H1：       `0a 10 <14-byte name> 32 0b ...`
```

H1 仍声明字符串长度为 `0x10`，导致后续 `32 0b` 被吞入名称值；名称字节虽然存在，按 wire 路径已
不能解码为合法字符串。主记录的外层 protobuf-like 结构仍可能被宽松递归扫描器部分解析，因此此前
“主记录存在、闭包完整”的自动断言没有发现此问题。结合用户反馈“列表和场景都没有”，这是当前
最直接、优先级最高的失败原因。结论范围是当前候选的确定编码缺陷；修正后游戏是否接受仍需重新实验。

第二，所有成功样本的主定义 ID 都在顶层 `field 6` 中出现；测试5经编辑器保存后也有该登记。H1 主 ID
`1077936137` 没有任何顶层 `field 6` 出现路径。它是否单独决定加载仍待最小候选验证，不能在当前
证据下与名称损坏合并为唯一根因。

第三，真实的 `1 → 2` 增长保持定义侧和实例侧名称分别为“装饰物_1”“装饰物_2”，测试3/4/5 均同构；
H1 的 7 条定义侧和 7 条实例侧辅助记录全部保留为“装饰物_2”。这是确定的生成差异，但因主元件已在
更早的名称 wire 处损坏，尚不能断言重复名称会单独导致整个元件不可见。

本轮只读检查同时修正了样本身份：早期测试2的定义/实例 ID 分别为 `1077936131/1077936133`，测试3
分别为 `1077936132/1077936135`；后续测试4/5才呈现定义与实例同 ID。数量比较不得假设所有时期都采用
同一 ID 形态。

### 19.1 三装饰物修复候选与游戏验证

按上述红灯修复后，以编辑器保存的测试5颜色样本为来源，重新生成 3 件装饰物的 `静态拼装H1`：

```text
主定义/实例 ID：1077936137
定义侧辅助：1073741850、1073741851、1073741852
实例侧辅助：1073741857、1073741858、1073741859
名称：装饰物_1、装饰物_2、装饰物_3
位置：[65, 0, 0]
候选 SHA-256：0563d31583606dc5e4d8dbb536c77965294b013c755f982f7a7db9fe0ec66e42
候选大小：48425 bytes
```

候选使用结构化字段重写名称，在主定义与主实例中都验证“静态拼装H1”可按 wire 路径解码；同时向
顶层 `field 6` 增加一次主 ID 登记，并保持 `field 9` 原始字节不变。写入实际目标后重新运行断言，确认
两侧 packed `field 501` 均为 3 项、名称连续唯一、owner/backlink/resource 闭合且旧辅助 ID 未增引。
写入前失败 H1 的保护备份为：

```text
1073741847.gil.before-static-assembly-H1-three-item-fix-20260725-140242.bak
SHA-256：b22c36b139fce1745e8c4f592c52b624ce06ff5a459db531a074129aa70b3206
```

用户随后在游戏中确认：3 个装饰物成功组成一个 H，实验通过。截图显示左右竖条可辨认，但横条偏低、
偏厚，并且当时可见的球体主模型与中间区域重叠，因此字母比例仍需调整。这个反馈证明当前 3 项闭包、
名称修复和登记组合可被游戏接受；不能单独拆分证明三项中的任意一项都是唯一必要条件。

用户随后在编辑器中把主球体换成空模型并保存，目的是让游戏只渲染装饰物。该回存样本已单独保护：

```text
SHA-256：c474fb32c836c1e13af1144cb05a8c53246c19b40436569d7dbff984dd02df02
大小：48044 bytes
备份：1073741847.gil.after-H1-empty-main-user-save-20260725-142258.bak
```

以已游戏通过候选 `0563d315...66e42` 和该回存样本执行按路径 raw wire 差分后，确认编辑器对 H1
作了以下规范化：主定义与主实例资源均从 `10009002` 改为 `10005018`；主定义缩放从 `[3,3,3]`
改为 `[1,1,1]`；定义侧三件装饰物及其 ID 保持不变；实例侧三件装饰物内容保持不变但 ID 从
`1073741857..1859` 重分配为 `1073741863..1865`，并相应更新主实例 packed 列表。顶层 `field 9`
原始字节不变。主 ID 的 `field 6` 登记在回存后共有三条，类型码为 `100`、`100`、`400`，分布在两个
顶层组中。这是该地图和此次编辑器回存的真实 wire 观察，不构成通用 `.gil` schema 结论。

据此生成了仅位于临时目录的候选，来源哈希为 `c474fb32...df02`，候选哈希为
`0540f6f4f72962b4eabac002f994377090eae1c57c1bd690a7b88550d85ba2a8`。候选保持 H1 空主模型和
主/辅助 ID，左右竖条为 `x=-2/+2`、缩放 `[0.45,3,0.45]`，中横条为 `x=0`、缩放
`[2.2,0.2,0.45]`。新增独立 W1 的主 ID 为 `1077936138`，定义侧辅助 ID 为
`1073741853..1856`，实例侧辅助 ID 为 `1073741866..1869`；四件长方体分别位于相对
`x=-2.1/-0.7/+0.7/+2.1`，绕 Z 轴交替 `-25/+25/-25/+25` 度，缩放 `[0.35,2.2,0.45]`。
W1 主实例位置为 `[59,2.4474487,0.5680947]`，与 H1 分离。候选自动断言确认 H/W 的 packed 列表、
名称、owner/backlink、资源、定义/实例 Transform、三条 `field 6` 登记闭包均一致；仅顶层
`field 4/6/8/27` 改变，`field 9` 与所有其它顶层区域均原始字节不变。该结果是自动结构验证，
尚未写回或游戏内验证。

用户随后确认 H1 与 W1 都在游戏内正常显示。该反馈把本次候选升级为“自动结构验证通过 + 写回成功 +
游戏内验证通过”。截图同时显示 W 的四段仍未形成紧密的字形，说明可加载闭包已得到验证，但局部
Transform 的视觉调参需要从一次性脚本迁移到可重复配置工具。

### 19.2 静态模型拼装工具（当前实现）

> 状态：当前实现 / 已验证
> 来源：当前代码实现 + 自动回归 + 真实地图写回回读 + 用户游戏内验证
> 最近校验：2026-07-25
> 适用范围：以本地图 `静态拼装H1` 为模板、使用长方体资源 `10009001` 创建新组件；其它模板、资源和地图待逐项验证

当前实现入口为 `gsts assets:static-assemblies`，核心代码位于
`src/cli/gil_static_assemblies.ts`，命令包装位于 `src/cli/assets_static_assemblies.ts`。工具以
配置中的 `assets.staticAssemblies` 声明每件装饰物的本地 Transform。公开 `GstsConfig` 类型当前
尚未声明该字段，因此生产验证使用 `.mjs` 配置，不添加 `GstsConfig` 类型注解。`position`、
`rotation`、`scale` 都直接使用游戏单位；对原始长方体模型，`scale: [1,1,1]` 对应游戏中
`1×1×1` 的原始尺寸。每件 `position` 相对组件原点，组件自身 `position` 是场景位置，两层坐标
不能混用。

```js
const config = {
  // 其它普通 gsts 配置省略
  assets: {
    staticAssemblies: [
      {
        name: '字母A',
        prefabId: 1077936140,
        templatePrefabId: 1077936137,
        templateName: '静态拼装H1',
        position: [72, 0, 0],
        items: [
          {
            resourceId: 10009001,
            position: [-1, 0, 0],
            rotation: [0, 0, -20],
            scale: [0.2, 2, 0.2]
          },
          { resourceId: 10009001, position: [1, 0, 0], rotation: [0, 0, 20], scale: [0.2, 2, 0.2] },
          { resourceId: 10009001, position: [0, 0, 0], scale: [1, 0.15, 0.2] }
        ],
        definitionAuxiliaryIds: [1073741878, 1073741879, 1073741880],
        instanceAuxiliaryIds: [1073741881, 1073741882, 1073741883]
      }
    ]
  }
}

export default config
```

默认命令只在临时文件中生成并输出摘要，不会修改地图：

```bash
node bin/gsts.mjs assets:static-assemblies --config gsts.static-assemblies.config.mjs --map-id <mapId>
```

`--output <candidate.gil>` 只允许新建候选文件；`--write` 才会覆盖已选地图，并在目标同级
`.gsts/backups/` 创建备份。创建操作必须显式提供 `prefabId`、定义侧辅助 ID、实例侧辅助 ID；工具先扫描
目标地图的 `field 4/8/27` 记录，遇到冲突 ID、模板缺失、辅助 ID 数量不等于 items 数量或模板缺少
`field 6` 登记时直接失败。它不会猜测下一可用 ID，也不会自动写回。

自动回归：`npx tsx tests/gil_static_assemblies.ts <map.gil> <templatePrefabId> <prefabId> <definitionStart> <instanceStart>`。
该回归证明输入文件不变、候选 bytes 发生预期变化以及重复 ID 被拒绝；不能替代编辑器/游戏验证。

自 2026-07-28 起，`GstsConfig.assets.staticAssemblies`、`GstsStaticAssembly` 和
`GstsStaticAssemblyItem` 已成为 npm 根入口公开类型，CLI 不再维护私有配置副本。preview 会打印来源与
候选 SHA-256、assembly/template/resource/辅助 ID 计划、候选大小、触及的顶层字段和
`writePerformed=false`；`--write` 另打印备份与写后哈希。中英文 CLI help 和项目模板已明确区分
静态 `.gil` 元件拼装、GIA NodeGraph 注入与运行时 `createPrefab`。这些是当前实现和自动构建证据，
本轮没有执行新的真实地图写回或游戏验证；颜色仍只继承所复制模板子项的未知字段，未公开任意颜色编码。

#### 19.2.1 第一轮生产验证

本轮以 SHA-256 `0540f6f4...ba2a8`、大小 `50400 bytes` 的真实地图只读副本为固定输入，使用
`静态拼装H1`（主 ID `1077936137`）作为模板，通过正式 CLI 创建 `静态拼装工具验证1`：

```text
新主定义/实例 ID：1077936139
定义侧辅助 ID：1073741870..1073741873
实例侧辅助 ID：1073741874..1073741877
组件位置：[72, 0, 0]
资源：四件长方体 10009001
局部缩放高度：2、1、0.5；第四件绕 Z 轴旋转 45°
```

候选 SHA-256 为 `067edfb3...8f3315`，大小为 `52704 bytes`。正式回归确认输入未改变且重复 ID
会被拒绝；独立 raw-wire 校验器确认容器、主定义/实例、两侧 packed 列表、4+4 辅助记录的名称、
资源、owner/backlink、定义/实例 Transform 和三条 `field 6` 登记均闭合。候选只改变顶层
`field 4/6/8/27`，`field 9` 原始 bytes 不变。

用户明确确认后，CLI 使用 `--write` 从同一真实目标重新生成并写回，在目标同级 `.gsts/backups/`
建立 SHA-256 仍为 `0540f6f4...ba2a8` 的备份。写后目标与候选哈希一致，独立校验器对真实目标回读
再次全部通过。用户随后确认游戏内验证通过：元件列表和场景可见，三根竖条呈现计划的相对高度，
第四根长条呈现 45° 旋转，且组件与已有 H/W 分离。

该结果证明当前工具在上述地图、模板、资源和四件装饰物配置下完成了“候选生成 → 独立回读 → 备份
写回 → 写后回读 → 游戏验证”的生产闭环；不证明其它地图、模板或资源可直接复用，也不改变显式 ID
分配、目标哈希复核和写回前用户确认要求。

写回前仍须展示目标哈希、新主/辅助 ID、位置、Transform、候选哈希和回滚方案，并获得新的明确确认。

## 20. 维护记录

| 日期 | 变更 | 证据 | 状态 |
|---|---|---|---|
| 2026-07-24 | 建立静态模型 `.gil` 研究文档；登记 `1073741847.gil` 中的定义区、实例区、Transform、来源引用和装饰物实例 | 真实地图只读扫描；用户编辑器操作说明 | 待验证 |
| 2026-07-24 | 追加式静态模型实验：从现有球体、自定义球体、自定义球体2实例复制 7 个副本，仅修改实例 ID 和 `field 6` Transform；原实例与定义区未改 | 写回前 SHA-256 `f273ed0e...b90d07`；写回后 SHA-256 `3acf83a2...632022`；`8.1` 实例数 5 → 12；备份文件已生成 | 已写回、基础球体部分成立；自定义元件/装饰物失败 |
| 2026-07-24 | 用户反馈基础球体正常；未看到新测试球体测试；新测试球体2的装饰物列表为空。只读复核确认追加实例存在但 `field 4` 定义区未增加、`装饰物列表` 未出现 | 用户游戏/编辑器反馈 + 写回后 wire 扫描；当前文件 SHA-256 `3acf83a2...632022` | 已验证失败边界，待真实 A/B 样本 |
| 2026-07-24 | 恢复到原始样本后，复制“新测试球体”和“新测试球体2”的定义记录，分配新定义 ID；追加 2 个新自定义组件实例和 4 个球体装饰物实例 | 写回前 SHA-256 `f273ed0e...b90d07`；写回后 SHA-256 `c7a06508...0ece70`；`field 4` 4583 → 5700 bytes；`8.1` 实例数 5 → 11；备份文件已生成 | 用户反馈失败：新增对象仍按基础球体识别 |
| 2026-07-24 | 用户确认参考自定义元件 ID：`新测试球体 = 1077936130`、`新测试球体2 = 1077936131`；上一轮错误使用 `1090519042/1090519043` 作为新定义 ID，追加记录未被游戏识别为新元件 | 用户编辑器/游戏反馈 + 当前文件只读 ID 扫描；新增对象名称仍为球体、基础资源 ID 未改变，Transform 正常 | 已确认错误原因方向，待真实 A/B ID 分配样本 |
| 2026-07-24 | 按用户要求恢复到上一轮实验前原始地图，确认后续比较路线固定为“球体 → 新测试球体 → 新测试球体2”，并记录元件 ID 连续递增规则 | 恢复后 SHA-256 `f273ed0e...b90d07`；恢复前实验版另存为 `before-restore-route-135603.bak`；用户提供 ID 规则 | 已恢复，待按三层路线重新分析 |
| 2026-07-24 | 用户新增真实样本“新测试球体3”：基于“新测试球体2”，在原装饰物列表中增加基础元件“长方体” | 元件 ID `1077936132`；场景实例 ID `1077936135`；`field 4` 4583 → 5151 bytes；`field 8` 实例 5 → 6；新定义长度 565、实例长度 585；长方体资源 ID `10009001` | 已观察，待实验验证 |
| 2026-07-24 | 以真实“新测试球体3”定义和实例为模板，创建连续元件 ID `1077936133`、名称“新测试球体4”，并追加实例 ID `1077936136`，位置 `[40,0,0]` | 写回前 SHA-256 `8c8dfead...7681a7`；写回后 SHA-256 `4ce7d704...9590eb`；备份 `before-component4-20260724-141020.bak`；自动扫描确认新定义/实例和嵌套结构 | 用户反馈：元件生成和 ID 正常，但装饰物丢失 |
| 2026-07-24 | 以用户确认完整正常的“新测试球体4”（元件/实例 ID `1077936135`）为模板，创建“新测试球体5”，严格保持定义 ID、实例 ID、`field 2.field 1` 三者同值为 `1077936136`，位置 `[50,0,0]` | 写回前 SHA-256 `2e19460f...4862b6ea`；写回后 SHA-256 `c6a16977...2245ee485`；备份 `before-component5-20260724-143627.bak`；自动扫描确认定义、实例、名称和结构 | 已写回，待用户游戏验证 |
| 2026-07-25 | 以已游戏通过的“新测试球体5”双装饰物闭包为模板，生成 `静态拼装H1`：新主 ID `1077936137`，7 个长方体装饰物，定义侧辅助 ID `1073741850..1856`、实例侧辅助 ID `1073741857..1863`，主位置 `[65,0,0]`；逐件 Transform 使用稀疏 wire 编码，颜色逐件复制当前真实样本的两种 `field 32` 原始样式 | 写回后 SHA-256 `b22c36b139fce1745e8c4f592c52b624ce06ff5a459db531a074129aa70b3206`；只读复扫确认主定义/实例、14 条辅助记录、两侧 packed `field 501` 的 7 项列表、resource/owner/backlink 和 Transform；`field 9` 原始字节及 NodeGraph 摘要未变 | **写回成功；旧自动验证存在盲区；用户确认元件列表和场景中都没有 H1，游戏内验证失败。** 后续只读 wire 诊断已定位主名称长度损坏，并另见 `field 6` 登记缺失及装饰物名称重复；不能将双装饰物闭包推广为任意数量装饰物。 |
| 2026-07-25 | 修复 H1 名称 length-delimited wire，补顶层 `field 6` 登记，将辅助名称设为连续唯一，并缩小为 3 件装饰物 | 候选/写入 SHA-256 `0563d315...66e42`；实际目标回读断言全部通过；用户游戏截图及明确反馈确认 3 件装饰物组成 H | **自动结构验证通过、写回成功、用户游戏内验证通过。** H 的比例仍需调整。 |
| 2026-07-25 | 用户在编辑器中把 H1 主球体换成空模型并保存，以便游戏只显示装饰物                                                                                                                                                                                                                       | 回存 SHA-256 `c474fb32...df02`、大小 48044 bytes；保护备份 `after-H1-empty-main-user-save-20260725-142258.bak`                                                                                                                                | 真实编辑器操作样本已保护；后续 raw diff 确认空模型资源为 `10005018`、定义侧辅助不变、实例侧辅助重分配；范围仅限该地图/回存样本。                                                                                  |
| 2026-07-25 | 基于空模型回存调整 H 比例并新增独立 W1 的候选                                                                                                                                                                                                                                          | 候选 SHA-256 `0540f6f4...ba2a8`；写回后回读断言 H 空主模型和 ID 保持、W 4+4 辅助闭包、三条登记、Transform、`field 9` 与未触及顶层区不变；用户提供 H/W 截图和明确反馈                                                                          | **自动结构验证通过、写回成功、用户游戏内验证通过。** H/W 均正常显示，但拼接视觉比例仍待调参。                                                                                                                     |
| 2026-07-25 | 新增配置驱动的 `assets:static-assemblies` 工具                                                                                                                                                                                                                                         | `npm run build`；临时副本上的 preview/output 回归；`tests/gil_static_assemblies.ts` 验证源文件不变和重复 ID 拒绝                                                                                                                              | **当前实现和自动回归通过；未以新工具写回或游戏验证。** 长方体 `10009001` 的闭包来源有 H/W 游戏证据，其它资源仍待验证。                                                                                            |
| 2026-07-25 | 使用正式 `assets:static-assemblies` CLI 创建四件长方体组件 `静态拼装工具验证1`                                                                                                                                                                                                          | 来源 SHA-256 `0540f6f4...ba2a8`；候选/写后 SHA-256 `067edfb3...8f3315`；正式回归与独立 raw-wire 校验通过；CLI 自动备份；用户确认三档高度、45° 旋转和场景分离均符合预期                                                                       | **候选自动验证通过、写回成功、写后回读通过、用户游戏内验证通过。** 结论仅覆盖当前地图、H1 模板、资源 `10009001` 和本次四件配置。                                                                                  |
