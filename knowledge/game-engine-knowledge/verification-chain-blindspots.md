# 验证链盲区族（verification chain blindspots）

六类单层结论冒充全链结论的盲区：注入失败看不见/读图不核数据流/离线不等于真实/逻辑不等于渲染/修复引入回归/管道判退；统一交付前验证清单 C1。

<!-- CLAIM:START clm_20EC2BE4048E84485B4F7A4FD5 -->

### 验证链盲区族：六类单层结论冒充全链结论 + 修复交付前验证清单 C1（08-22~08-29）

分层证据下每层只证明本层，08-22~29 六类盲区：①注入失败看不见（注入输出被 tail -2 截断漏 [error]，2905/2906 两次复测才发现修复从未注入）；②读图只核分支结构、不核条件节点数据流来源（08-26 LessThan 输入仍是 Addition 误判'已注入'）；③离线验证不含运行时语义（2964 越界读、2976 握手漏发离线全绿游戏全坏）；④只盯逻辑值不盯渲染（08-28 带球逻辑正确渲染错）；⑤修 bug 引入更严重回归（2956 黑块）；⑥管道判退（npm run x 2>&1 | tail 退出码=tail 的 0，08-29 再犯）。统一修复交付前验证清单 C1 六项：注入输出整行核对+退出码；读图核验含条件节点数据流来源；回读调用方 call node inputs/outputs 无悬空引脚；离线验证显式标注不含清单；改动执行链后提醒用户看渲染；回归先 diff 最近一轮改动。

#### 适用边界

与 clm_FE7C817C（GIA 生产链路五层）、clm_01KYF24E（写回分层验证）互补：本 claim 聚焦'盲区族'六型与 C1 清单。

<!-- CLAIM:END clm_20EC2BE4048E84485B4F7A4FD5 -->
