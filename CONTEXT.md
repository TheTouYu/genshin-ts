# CONTEXT — Genshin-TS 玩法领域术语表

> 领域语言（ubiquitous language）精确定义。只含术语，不含实现细节。
> 决策记录见 `docs/adr/`。

## 术语

### 角块（Corner Cubie）
2×2 魔方的 8 个小立方体之一（2×2 的 8 个方块全部是角块，与魔方专业术语 corner cubie 一致；3×3 起还有棱块/中心块，届时按 WCA 扩展）。每个角块是独立的场景实体（非宿主装饰物），因为运行时旋转以实体为单位。一个角块属于 3 条旋转层（X/Y/Z 轴各一条）。
_Avoid_: 方块（Cell，旧称，2×2 中与角块同义，已弃用）

### 旋转层（Layer）
绕某条轴旋转时一起转动的 4 个方块集合。2×2 魔方共 6 条层（3 轴 × 2 侧）。层是"选择哪些方块转"的逻辑分组，不是场景对象。

### 旋转操作（Rotation Move）
对一条旋转层执行的一次离散旋转。2×2 魔方共 6 种标准操作：绕 X/Y/Z 轴各 ±90°（R/L/U/D/F/B），这是 2×2 的全部旋转。不做任意角度——非 90° 旋转后魔方结构失效。3×3 及以上按魔方专业领域定义（WCA 符号体系）扩展，见 ADR-0002。分派：选项卡序号 1~6 对应 R/L/U/D/F/B（已游戏核验闭合）。

### 自旋（Spin）
角块绕自己中心的旋转，由匀速旋转运动器驱动（已验证：90°/1s 连续累积、世界轴语义）。层旋转时每个角块自旋 90°。

### 公转（Orbit）
角块绕旋转层轴线的圆周运动，即角块位置沿圆弧移动。官方无椭圆/圆弧运动器，用多段匀速直线逼近圆弧（每段一个直线运动器）。层旋转时每个角块公转 90°。

### 分段折线（Segmented Arc）
用 N 段匀速直线首尾相接逼近一段圆弧的技术：每段位移 = 圆弧上相邻采样点之差（弦向量），段数越多越接近圆弧。2×2 取 N=5（每段 18°）。

### 选项卡（Tab / Option）
实体上配置的选项组件。玩家选中某个选项卡后，实体挂载的节点图收到「选项卡选中时」事件（出参：事件源实体、选项卡序号、选择者实体）。这是玩法接收玩家输入的标准入口。

### 运动器（Mover）
运行时动态添加的组件，驱动实体运动。官方运动器共 6 类：匀速旋转、匀速直线、朝向目标旋转、定点运动（瞬间/匀速直线）、跟随运动、操控运动；无椭圆/圆弧轨迹类。一个实体可同时挂多个运动器（名称唯一标识）。运动器停止时触发「基础运动器停止时」事件（出参：事件源实体、运动器名称）——多段折线靠该事件驱动下一段。匀速旋转型参数：目标实体、名称、时长、角速度（角度/秒）、旋转轴朝向；匀速直线型参数：目标实体、名称、时长、速度向量。官方不支持节点图动态修改实体位置/大小（需销毁+重建），旋转走运动器。

### 输入锁（Input Lock）
布尔图变量，旋转进行中为真，期间忽略新的选项卡输入（旋转中再点会发生状态错乱）。旋转完成（最后一段运动器停止）后解锁。

### 元件（Prefab / Definition）
可复用的静态模型定义（root 4 definition + root 8 instance + root 27 装饰物闭包）。静态视觉模型应实现为单个自定义元件；玩法实体（需要运行时独立控制的部分）例外，拆成独立实体。

### 静态模型 vs 玩法实体
静态模型：纯视觉、整体操控 → 宿主+装饰物闭包。玩法实体：运行时被节点图独立控制 → 每个可控制单元一个实体。魔方属于后者（见 ADR-0001）：8 个角块各自是独立场景实体，因为旋转运动器以实体为目标，装饰物（静态闭包）无法被节点图单独旋转。

### 关卡实体（Stage Entity）
游戏必然存在的一个默认实体（DSL：`g.stage` / `getStageEntity`），可理解为"游戏开始了"的标志。起始状态挂在关卡实体上（不挂角色：多人游戏有多个角色，实体创建事件会重复触发）。

### 实体创建时（whenEntityIsCreated）
实体创建事件（出参：事件源实体、GUID）。角块在游戏开始时不预置在编辑器中，而是由节点图在「关卡实体创建时」动态创建（createPrefab），便于整体移位（改一个基准位置即可）。
