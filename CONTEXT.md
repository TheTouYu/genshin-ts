# CONTEXT — Genshin-TS 玩法领域术语表

> 领域语言（ubiquitous language）精确定义。只含术语，不含实现细节。
> 决策记录见 `docs/adr/`。

## 术语

### 方块（Cell）
2×2 魔方的 8 个小立方体之一。每个方块是独立的场景实体（非宿主装饰物），因为运行时旋转以实体为单位。一个方块属于 3 条旋转层（X/Y/Z 轴各一条）。

### 旋转层（Layer）
绕某条轴旋转时一起转动的 4 个方块集合。2×2 魔方共 6 条层（3 轴 × 2 侧）。层是"选择哪些方块转"的逻辑分组，不是场景对象。

### 旋转操作（Rotation Move）
对一条旋转层执行的一次离散旋转。2×2 魔方共 6 种标准操作：绕 X/Y/Z 轴各 ±90°（R/L/U/D/F/B），这是 2×2 的全部旋转。不做任意角度——非 90° 旋转后魔方结构失效。3×3 及以上按魔方专业领域定义（WCA 符号体系）扩展，见 ADR-0002。

### 选项卡（Tab / Option）
实体上配置的选项组件。玩家选中某个选项卡后，实体挂载的节点图收到「选项卡选中时」事件（出参：事件源实体、选项卡序号、选择者实体）。这是玩法接收玩家输入的标准入口。

### 运动器（Mover）
运行时动态添加的组件，驱动实体运动。匀速旋转型运动器参数：目标实体、名称、时长、角速度（角度/秒）、旋转轴朝向。官方不支持节点图动态修改实体位置/大小（需销毁+重建），旋转走运动器。

### 元件（Prefab / Definition）
可复用的静态模型定义（root 4 definition + root 8 instance + root 27 装饰物闭包）。静态视觉模型应实现为单个自定义元件；玩法实体（需要运行时独立控制的部分）例外，拆成独立实体。

### 静态模型 vs 玩法实体
静态模型：纯视觉、整体操控 → 宿主+装饰物闭包。玩法实体：运行时被节点图独立控制 → 每个可控制单元一个实体。魔方属于后者（见 ADR-0001）：8 个方块各自是独立场景实体，因为旋转运动器以实体为目标，装饰物（静态闭包）无法被节点图单独旋转。
