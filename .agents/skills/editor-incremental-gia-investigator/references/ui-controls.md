# UI 控件（root9 控件段）wire 规则速查

> 状态：部分已验证（真实 .gil 相邻快照差分）
> 来源：地图 1073741907（ui-inject-diff）图片控件差分（2026-08-22）；灯阵/UI 解析（2026-08-17/18）。
> 配套：本技能 SKILL.md「UI 差分实战经验」；知识书 `docs/game-engine-knowledge/ui-controls.md`。
> 注意：root9 同时承载**素材段**（素材库）与**控件段**（屏幕控件），两者都是 root9 field 502 记录，
> 但记录结构不同——`assets:ui list` 会把素材库记录误读为控件（名字「图片」的组被当成控件），
> 差分时先按记录结构区分，不要只看 root9 field 502 数量。

## 已闭合规则

### 图片控件 = 模板 + 实例 两条记录（2026-08-22 差分闭合）

添加一个图片控件在 root9 新增两条 502 记录：

| 记录 | 特征 | 说明 |
| --- | --- | --- |
| 模板（definition） | 无 `f504`；`f502[type4]` 自标识 | 图片控件本体，含图片源配置 |
| 实例（instance） | `f504` = 布局 ID；`f502[type3]` = `{f13={f501=模板ID}}` | 引用模板，注册进布局 |

实例 `f502[type3].f13.f501` → 模板 ID；模板 `f502[type4]` 自标识。

**双向引用**：模板 `f502[type4].f14.f501` = packed 实例 ID 列表（back-reference），
布局引用时追加新实例 ID。

### 官方预制控件 vs 自定义模板（2026-08-22 第五轮差分闭合）

| 路径 | 记录数 | 结构 |
| --- | --- | --- |
| 自定义模板 | 2 条（模板+实例） | 模板 `f502[type4]` 无 `f504`；实例 `f502[type3]` `f504`=布局 |
| 官方预制控件 | 1 条 | `f502[type5]` 单条，`f504`=布局，直接含图片源 `f505[f502=38]` |

图片源引用素材路径两者一致：`f505[f502=38].f503.f31.f6.f4` = 素材索引 ID。

### 布局引用规律（2026-08-22 差分闭合）

「把控件模板放进布局」= 三步联动：
1. 新建实例记录（`f504`=布局 ID，`f502[type3]` 引用模板）；
2. 实例 ID 追加进布局的 `f503` packed 列表；
3. 模板 `f502[type4].f14.f501` 追加实例 ID（back-reference）。

- 默认布局 `1073741825`：`f503` = 14 官方预置 + 用户新增实例。
- 控件组容器 `1073741840`：`f503` = 尚未放入布局的实例列表。

### 图片源引用素材的 wire 路径（核心）

```
f505[f502=38]            ← 图片源组件（type 38）
  f503
    f31 (msg)            ← 图片源 = 静态引用
      f3 (msg) = {f501: -1}   ← 无颜色覆盖
      f4 (varint) = -1        ← 无颜色覆盖
      f6 (msg) = {f2:1, f3:8, f4:<素材索引ID>}  ← 素材引用
      f10 (bytes 0B)
    f501 = 22
    f502 = 38
    f503 = 1
    f504 (msg) = {f2:1, f3:8, f4:<自身ID>}  ← 自引用
```

**素材索引 ID（= 素材库容器 ID）存在 `f505[f502=38].f503.f31.f6.f4`。**

> 两个独立样本确认（2026-08-22）：`f6.f2=1`、`f6.f3=8` 是固定常量；`f3={f501:-1}`、`f4=-1` 是「无颜色覆盖」标记。

### 控件记录通用结构（2026-08-17/18 灯阵/UI 解析）

- 控件记录顶层：`f501`=控件 ID、`f502`×N（组件槽，type 码区分）、`f504`=布局 ID（实例才有）、`f505`×N（内容/载体/图片源等）。
- `f505` 每条带 `f501`（序号）+ `f502`（type 码）+ `f503`（内容）。type 码：15=名字、12=载体（transform 槽）、38=图片源、23=其他。
- 控件位置在 `f505[f502=12].f503.f13.f12.f501[n].f502.f504` = `{f501:X, f502:Y}`（f32 fixed32，屏幕中心相对偏移）。
  尺寸在 `f505[f502=12].f503.f13.f12.f501[n].f502.f505` = `{f501:W, f502:H}`。
  坐标换算：wire 偏移 = 编辑器绝对坐标 − (800, 450)（1600×900 设计分辨率）。
- 布局 packed 列表：`layout.503` 是 packed varint（控件 ID 列表），用 `readVarint` 流式读，不要用消息解析器。

## 未闭合（OBSERVED，勿当规则用）

- `f3={f501:-1}`、`f4=-1` 配置颜色后如何变（颜色覆盖 wire）——需配置颜色后差分。
- 图片源为「动态引用/道具/状态/技能/单位状态」时的 wire 差异。
- root21/root29 编辑器自动登记语义（CLI 创建控件后编辑器首存会补，尚未闭合）。
- 图片控件在游戏内是否真的显示该素材——需游戏核验。

## 差分要点

- 图片控件差分：先 `compare-gil-root-wire.py` 确认只有 root9（+root46 审计噪声）变化，再定点 diff root9 field 502 记录集合差，定位新增记录，最后沿 `f505[f502=38].f503.f31.f6` 解码素材引用。
- 等长内容变更（如改名字/内容）记录总长不变，必须逐字节记录 diff，不能只看 root 层大小。
- 通用递归解析会把 packed/原始字节误当消息——只沿已知路径替换，未知子块保留原始字节。

## 悬浮交互页（2026-08-26 ui学习 1073741909 轮1 差分闭合）

> 样本：`.gsts/diff-exp/ui学习/1073741909.before-轮1-e3c1c1df.gil`（hash e3c1c1df，21234B）
> → `.gsts/diff-exp/ui学习/1073741909.after-轮1.gil`（hash 354bd144，25060B）。
> 编辑器变化：界面控件组编辑窗口添加「悬浮交互页」（全默认，未进详情编辑）。

### 已闭合规则

1. **添加悬浮交互页 = 6 条新 root9 502 记录**（无删除）：
   - 模板（type4 自标识，`f502[type4].f14.f501` = packed back-ref [实例ID]）
   - 实例（type3，`f502[type3].f13` = packed [模板ID]，`f504` = 控件组容器 1073741840）
   - 固有容器素材组 ×2（type6 槽 `f12.f501=2`，`f503` = packed 成员素材 ID 列表，显示名在 **t43 组件** `f501`=UTF-8「固有容器」——不在 t15，`assets:ui list` 因此显示 `(空)`）
   - 交互页关闭按钮素材 ×2（type6 槽 `f12.f501=3`，`f504` = 所属素材组）
2. **控件种类标识**：t44 组件 = `{f502: 330001}`（悬浮交互页 kind 常量；模板与实例一致）。
3. **固有容器引用**：t42 组件 `f503.f501` = varint(素材组ID)。模板 t42 → 一组、实例 t42 → 另一组（模板/实例各带一份固有容器组）。
4. **顶层索引**：root9 f501 = packed varint 列表 = 所有 parent=None 记录 ID（模板/布局/容器/素材组；不含实例与素材）。编辑添加模板插最前、素材组追加在后。
5. 容器 1073741840 `f503` packed 追加实例 ID（96B→104B）——三步联动第 2 步在控件组容器上同样成立。
6. f505 组件通用骨架：`f501`=序号、`f502`=type 码、`f503`=内容、`f504`=`{f16:1, f24:8, f32:自身ID}` 自引用（图片控件里是 `{f2:1,f3:8,f4:ID}`，字段号随上下文不同，三元组 1/8/ID 相同）。
7. 载体 t12 组件含 4 个设备槽（键鼠/移动触屏/主机手柄/移动手柄）`f503.f13.f12.f501[n]`。

### 未闭合（OBSERVED，勿当规则）

- t40 `{f33:0}` / t41 `{f34:0}` 空组件语义（候选：页面容器列表/初始生效/形式变量）。
- 模板与实例为何各带一份固有容器组。
- 关闭按钮素材内部样式 id：t50 `f43.f502.f503=300009`、`f43.f504=260326`、t52 `f44.f503=50888`。
- 素材段 root9 f501 与素材库（素材索引 ID `0x40000000+`）的关系待样本。

## 悬浮交互页内页签/单选项视窗（2026-08-26 轮2 差分闭合）

> 样本：after-轮1.gil（354bd144）→ after-轮2.gil（924e5b00，27251B）。
> 编辑器变化：悬浮交互页详情编辑中添加 1 个页签（默认）+ 1 个单选项视窗（默认）。

### 已闭合规则

1. **页签/视窗各为 1 条新 root9 502 记录**，`f504` = **实例的固有容器素材组**（1073741844，不是模板的 1073741847）；组 `f503` packed 列表追加成员（→ [关闭按钮, 页签, 视窗]）。模板/实例记录本体不变 → **详情编辑内容全部挂在实例侧固有容器组**。
2. **页签 kind = 320001**：t49 组件 `f503.f41 = {f503: 320001}`（f41 是 varint 320001 的封装）。**单选项视窗没有 32xxxx/33xxxx 常量**——身份由组件结构区分（视窗专有 t59；页签专有 t46+t49）。
3. 页签组件：t15 名字/t23/t12 载体/t46(3 组 f32 尺寸对)/t47(页签项配置：默认项名「页签_1」+「页签文本」)/t48/t49(kind)/t58(样式列表「样式_1」)/t37。
   视窗组件：t15(名称为空)/t23/t12/t59(4 组 f32 尺寸对+f508=1)/t47(默认项名「列表_1」)/t48/t58/t37。
4. t46/t59 的尺寸对 = `{f501: fixed32 X, f502: fixed32 Y}`（50×50/30×30/24×24 等）= 状态素材尺寸（默认/悬停/按下[/选中]）。
5. t48 共享常量 `f41.f507 = 50907`（页签+视窗都有）——候选：滚动方向/交互默认值。
6. f504 自引用三元组 `{f2:1, f3:8, f4:自身ID}` 与 `{f16:1, f24:8, f32:自身ID}` 并存——字段号随封装消息不同，三元组语义相同。

### 未闭合（OBSERVED）

- 视窗 kind 编码方式（无常量，结构区分——待更多样本确认是否藏在 t47 内部）。
- t47 页签项/列表项配置内部含 **wire type 3（group）** 编码，现有 walker 不支持——差分脚本需支持 group 或保留原始字节（页签 t47 有 group、视窗 t47 对应位置是空 f13）。
- 50907 语义；t58「样式_1」结构；t37 与关闭按钮 t37 相同的 4 个布尔/字节字段语义。

## 悬浮交互页形式变量（2026-08-26 轮3 差分闭合）

> 样本：after-轮2.gil（924e5b00）→ after-轮3.gil（e05cffed，29698B）。
> 编辑器变化：形式变量管理添加 4 个变量（4 种类型，第 4 个=动态文本域）。

### 已闭合规则

1. **t41 组件 = 形式变量列表**（模板 1846 与实例 1843 双写，各 +99B，内容一致）：`t41.f503.f34` = 变量条目序列，每条 = `{f501: {f501: 类型码, f502: 名字UTF-8}, f503: 序号}`。类型码缺失 = 0（编辑器第 1 种类型）。
2. 类型码映射（用户口述 + wire 互证）：**0=整数、1=浮点数、2=字符串、3=动态文本域**（用户 2026-08-26 确认编辑器顺序）。
3. **动态文本域语义（千星知识库 FAQ，bbs-faq-since1124）**：动态文本域只是**文本类型**——文本可随变量更新实时变化显示；文本框/文本气泡都是动态文本域；新版本页签、单选项视窗、自定义按钮等也支持；选项卡也是动态文本域；静态文本域=不能随变量更新、一般不能插入变量。
3. 添加变量后，编辑器给页签/视窗记录**补上 type6 槽 f12 子类型码**（页签=4、视窗=5；此前已见 1=模板/实例、2=素材组、3=素材），并把页签/视窗**完整复制**进模板的固有容器组（组 1847 f503 = [1848, 1851, 1852]；副本与原件逐字节相同，仅 f501 ID 与 f504 parent 两处不同）。
4. root9 f501 顶层索引不变（副本 parent=1847 非顶层）——再次验证「f501 = parent=None 记录 ID 索引」。

### 未闭合（OBSERVED）

- 编辑器为何在添加形式变量时复制页签/视窗到模板组（候选：模板侧定义视图；触发条件待归因）。
- 动态文本域变量的「用法」wire 侧形态（待用户教学 + 页签项绑定实验）。

## 悬浮交互页内自定义按钮（2026-08-26 轮4 差分闭合）

> 样本：after-轮3.gil（e05cffed）→ after-轮4-自定义按钮.gil（8f52c17a，30918B）。
> 编辑器变化：详情编辑添加 1 个自定义按钮（状态未配置，默认）。

### 已闭合规则

1. **自定义按钮 = 1 条新 root9 502 记录**，f504 = 实例固有容器组 1844（组 f503 → [关闭按钮, 页签, 视窗, 自定义按钮]）；**无 type6 槽**（页签/视窗的 type6 是加形式变量时补的，按钮没有）。
2. 按钮组件集 = t15 名字 + t23(文本/空) + t12 载体 + **t52/t54/t50/t37**（与关闭按钮同款）+ 专属空组件 **t62/t63/t64**。
   - t52 `f44.f503=50888`：预设按钮样式 id（关闭按钮与自定义按钮共用 → 默认样式 50888）。
   - t50 `f43`：样式结构本体——自定义按钮 `f43.f501=1`、`f43.f502` 56B（含 UTF-8 标签「文本内容」+ 常量 101001/300001）、`f43.f503` 168B 状态素材、`f43.f504=260326`；关闭按钮 `f43.f502` 38B 含 300009。
   - t62/t63/t64 空组件：候选 = 关联监听页签/视窗、快捷键、返回服务器事件（未配置 → 空）。
3. **按钮无 kind 常量**（与视窗一致，身份靠组件结构）。
4. **root6 首次变化**：追加 33B folder 条目 `{f2: {f1:"root", f3:1}, f3: {f1:"未分类页签", f3:2}}`（root 6 folder 条目结构，见 node-graph-creation 模块；样本不足，语义待更多轮次）。

### 未闭合（OBSERVED）

- 交互项索引（事件出参）在按钮记录中的编码——需配置「返回服务器事件」后差分。
- 「未分类页签」文件夹的注册时机与用途；t62/t63/t64 语义；101001/300001/260326/50888 常量语义。
