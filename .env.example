# Local-only embedding configuration. Copy to .env and fill the API key.
VECTORENGINE_API_KEY=
VECTORENGINE_BASE_URL=https://api.vectorengine.ai/v1
VECTORENGINE_EMBEDDING_MODEL=text-embedding-3-small
