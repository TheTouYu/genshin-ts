# 进度记录 — 真实足球

## 最近轮次

- 2026-08-30（R2-R3 注入完成 + 读图核验通过，待游戏验证）：
  - **注入**：注册信号 ball_dropped(vel:vec3)（send 1610612750/mon 1610612751/srv 1610612752）+ 注入 game.gia→1073741825（69 节点）+ dribble-field.gia→1073741828（12 节点）；Temp 同步双 SHA 9e086d47。
  - **核验（逐项勾对）**：check-gil-composite-refs --incoming ✓（game.gia 的 7 项=文档化误报：2000000000 段旧段复用+信号 def；dribble-field.gia 0 悬空）；parse --list 复合目录 0 残留（旧 phys_tick/physRollFriction 被同 ID 覆盖清除，无 (1) 后缀；唯一注意=跨 .gia 双份 dribble_field_get_role 0000/0021 均活引用，非残留）；scan-gil-var-pins 30 图 205 节点全完整；explain 读回 game（5 入口/分发链/心跳门控/ball_dropped/单一提交）、dribble_field_tick（物化→脱脚信号→写回→牵引分支）、phys_fly_tick（线性链+tmpGoal 物化+goal_reset+state 双写提交）、state_commit（2 节点双写）全部符合设计；implTotal 776<3000。
  - **用户测试点**：①走近球 1.2m 内自动带球（CARRIED，≤0.5s）②带球贴地/跟手/停球也停 ③射门 9 选项/弧线/高速落地滑动段 ④**射门停球后再走近可重新带球（R0 铁证#1 修复回归点）** ⑤进球计分+2s 自动复位 ⑥脱脚（跑远/急停甩球）⑦墙/门柱/反弹/马格努斯/运动中冲量 回归；日志 Beyond_Debug_Log 供帧值分析。
- 2026-08-30（R1-R3 状态机重构·代码实施完成，编译/注入待编译器稳定）：
  - **R1 定稿**：状态同步=轮询检测（lastSeenState）；脱脚=ball_dropped(vel) 信号；state 单一提交（state_commit / tick 链尾提交对）；物理 tick 自提交消灭多 outflow 漏写；SLIDE 按物理定义补入口（落地高速>4.5m/s + 自旋收敛）；GOAL 状态 + 2s 复位（goalNew 物化 tmpGoal 防重复求值）；心跳 0.5s 常驻门控。
  - **代码改动**：game.ts 重写（状态机唯一仲裁/宿主分发/心跳/ball_dropped 落地/GOAL 复位）；physics.ts（三 tick 数据选择+单一提交、SLIDE 入口与自旋收敛、删 phys_tick/physRollFriction）；kick.ts（移除全部 state 写入）；新增 state.ts（stateCommit/statePossessCheck）；dribble-field.ts 复合+根图（CARRIED 门控、进入复位、脱脚信号入复合）；signals.ts 只留 ball_dropped；删除 dribble.ts/debuglog.ts。
  - **编译器并发回归**：根图数组字面量 → gsts.f.assemblyList 触发 ctx 错误（同事修复中；旧代码同样失败已实证）；已用显式 f.assemblyList + setNodeGraphVariable 规避；dist 构建被并发 build 干扰（config_loader.js 缺失），编译暂缓。
  - **下一步**：编译器稳定后编译 → 解码核验 .gia → 注入（用户确认）→ R4 游戏验证。
- 2026-08-30（R0 状态机重构·现状盘点，方案待实施）：
  - **任务**：状态机架构重构——引入 CARRIED + 状态机唯一仲裁 + 行为图职责单一化（任务书见会话；方案见 `examples/football/REFACTOR-PLAN.md`）。
  - **R0 交付**：状态所有权矩阵 / 状态机判定表草案（FREE/CARRIED/FLYING/ROLLING/SLIDE/GOAL）/ 图间接口方案（state 自定义变量唯一写者 + state_changed/ball_dropped 信号）/ 文件改动清单——全部写入 REFACTOR-PLAN.md。
  - **关键铁证（真实地图 1073741908.gil + IR 双印证）**：phys_tick 复合 4 个 done outflow 只有 outflow[0]（FREE 分支）被调用点消费 → FLY/ROLL/SLIDE tick 后自定义变量 state 镜像不更新 → 球滚停后带球图不恢复（软契约失效根因）；速度双真相（game.ballVel vs dribble.ballVx/Vz）；SLIDE 无入口（死分支）；GOAL 非状态；_GSTS_input/_GSTS_dribble 死图未挂载。
  - **决策（用户确认）**：SLIDE 保留按物理定义设计（落地高速>4.5m/s 进 SLIDE）；CARRIED 用范围查询（1.2m+球速低，FREE 心跳 0.5s）；删除输入图（tab 由状态机图直收）；第 9 选项保留复位；新增 GOAL 状态 + 2s 自动复位；文档定调速度场吸附。
  - **下一步（R1）**：双节拍最小实验（0.5s 心跳 vs 0.12s tick 协同语义）→ 判定表/接口定稿 → R2 状态机图落地。

## 基本信息

| 项 | 值 |
|---|---|
| 地图 | 1073741908「真实足球」（2026-08-22 重建：原 1073741901 被删除后重建，资产配置复用） |
| 占位节点图 | 1073741825「足球玩法」 |
| 编译配置 | `examples/football/gsts.config.ts`（待创建） |
| 元件 | 足球场白线 1077936129、足球门 1077936130、足球 1077936131 |
| 场景实体 | 足球场(1077936132) / 左球门(1077936133) / 右球门(1077936134) / 足球(1077936135) |

## 阶段状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 规划 | 玩法设计、球场规格、资产选择 | ✅ 完成 |
| P1 地图/目录 | `examples/football/` + maps:create + 占位图 | ✅ 完成 |
| P2 资产导入 | 足球场白线(FIFA 105×68)、足球门(×2)、足球(升级4全开) | ✅ 写回成功 |
| P3 视觉核验 | 用户编辑器/游戏查看（两轮弧线修复后） | ✅ 用户确认"看起来比较正常" |
| P4 输入机制 | 操作台实体 + tabBar 9 选项（sphere r=3）+ 输入图 | ✅ 写回成功（待游戏核验） |
| P5 玩法逻辑 | 多图 + 复合化重构（物理图 13 节点 + 输入图 2 节点 + 11 复合） | ✅ 编译注入成功（待游戏核验） |
| P6 沉淀 | 文档/技能/提交 | ⬜ |

## 变更记录

- 2026-08-22（架构重构：多图 + 复合化，阶段 0 完整跑通）：
  - **多图拆分**：输入图 `input.ts`（1073741826，挂操作台实体 1077936137）+ 物理图 `game.ts`（1073741825，挂球实体 1077936135），跨图用信号 `football_kick`（tabId:int）。
  - **复合化**：221 节点"面条图" → 物理图 13 节点 + 输入图 2 节点 + 11 个复合（motion×2 / kick×3 / phys×6），implTotal 178 < 3000。
  - **元件/实体**：新建操作台元件 1077936136 + 实体 1077936137（空模型 + tabBar 9 选项 sphere r=3）；球元件 1077936131 + 实体 1077936135 补 basicMotion（type 4）。
  - **信号**：注册 `football_kick`（sendId=1610612741/monitorId=1610612742/serverId=1610612743）。
  - **注入+挂载**：game.gia→1073741825、input.gia→1073741826；挂载球/操作台；Temp 同步（SHA 1727cedb…）。
  - **回读核验**：读图自检通过（主图/输入图/phys_tick 复合执行流正确）、变量 pin 42 全完整、信号发送1+监听1、复合引用 3 处"缺失"为信号定义单元误报。
  - **待游戏核验**：H1（瞬间移动）/H6（匀速直线 0.2s）/H7（马格努斯符号）/H8（运动器衔接）+ tabBar 触发。
- 2026-08-23（运动器与物理两轮修复）：定点运动器+旋转运动器同链互打断 → 直线设备秒停、球不动；改回匀速直线运动器（速度=(target-loc)/dt）。落地即滚吞掉反弹 → 加 |vy| 弹跳阈值；滚动摩擦 0.985→0.8；滚滑自旋按 ω=(v_z/R,0,-v_x/R) 实时推导。提交 6fdcfa3、48b680d；复盘 retrospective-2026-08-23-football-motion-and-rolling.md。 后续修复：施力首段视觉目标与物理积分不同步（v0·dt vs v1·dt）导致空中速度突变/虚拟天花板 → kickLaunch 复用 physIntegrate 预积分首步；提交 e23b817。 再修复：kickLaunch 多消费+先写回输入图变量致二次积分（低弹道扎草）→ 调整 setPos→physApplyMotion→setVel 顺序；复位 lockRotation=true 保留旧朝向致第二段旋转轴错 → 改 false；提交 9b0d261。 同族修复：physFlyTick/physRollTick 物化 tmpPos/tmpVel/tmpSpin 快照，消除 goal/ground 二次积分；提交 5f2fc97。 最终修复：上旋低平首段积分 y=-0.1 扎草 → kickLaunch 首段目标 max(y,0.25) 贴地 clamp（速度仍为 integ.nvel）；提交 60308a7，用户复测通过。 扩展：运动中再施力改为唯一名冲量运动器叠加（impulseSeq→str），修复同名 physics 冲突；提交 b23f7eb。回归修复：内层 doubleBranch 前放 dbgTag 复合调用破坏分支流 → 移入分支内；提交 4e8c55a。 修补：快静止 ROLLING 补力 y 被砍 0 → 保留 y 且补力后 state=1；提交 703a9ca。调参：8 选项 y 力度与绕场方向重排；提交 66321f1。修复：kickApplyImpulse 分支链 n7↔n8 循环控制流导致启动失败 → 线性化链；提交 341815c。
- 2026-08-21：创建 `examples/football/` 目录、地图 1073741901「真实足球」、占位节点图 1073741825。
- 2026-08-22：原地图 1073741901 在游戏目录中消失（疑被编辑器删除）→ 重建为 1073741908（maps:create + maps:init + 元件/实体写回 + 占位图 1073741825 复用）。
- 2026-08-21：从地图 1073741879「足球门」导出足球门(1077936150, 142 items) 和足球·升级4全开(1077936155, 224 items) 的真实闭包 items，重建为新地图的元件 1077936130 和 1077936131。
- 2026-08-21：生成 FIFA 标准 105×68m 足球场白线（84 items：边线/底线/中线/中圈/禁区/小禁区/罚球点/罚球弧），写入元件 1077936129。
- 2026-08-21：导入 4 个场景实体：足球场（原点）、左球门（[-52.5,0,3.66] rot [0,90,0]）、右球门（[52.5,0,-3.66] rot [0,-90,0]）、足球（[0,0.247,0] scale 0.25 中心 y=半径贴地）。
- 2026-08-21：写回成功（候选 SHA 45b09afe…）、Temp 同步、回读核验通过。
- 2026-08-21（用户反馈后修复）：弧线旋转/方向修正——
  - 用户手动修改中圈 4 段弧线（只改旋转），比对真实 aux 差异（1073743151–3154）闭合旋转公式。
  - **根因**：旧公式「段中心角+90°」把长方体局部 X 轴转到切线的 **z 镜像**（绕 X 翻转），表现为自旋不对。
  - **正确公式**：切线 = (-sinθ, 0, cosθ)，由 (cos rotY, -sin rotY) = 切线得 `rotY = atan2(-cosθ, -sinθ)`（度）。
  - 罚球弧方向画反（凸向球门）→ 修正为凸向场内（右半场 -X / 左半场 +X），旋转 `rotY = atan2(-cosθ, side·sinθ)`。
  - 恢复备份（6b42e325）重新生成写回，最终候选 SHA `72cf176f…`；写后回读：中圈 32/32 段切线正确、双侧罚球弧凸向场内、Temp 同步。
- 2026-08-21（用户反馈后修复 v2）：罚球弧角度修正——
  - 用户反馈：罚球弧不是半圆，应正好与禁区横线相交，画多了会穿过横线。
  - **几何**：罚球点 x=±41.5（距底线 11m），禁区横线 x=±36（距底线 16.5m）；
    交点 `cosθ = |41.5-36|/9.15 = 5.5/9.15` → **θ_max = arccos(0.6011) ≈ 53.06°**。
  - **修复**：弧范围从半圆 `[-90°,+90°]` 改为 `[-θ_max, +θ_max]`（约 106°），
    注意必须用 `Math.abs(PEN_SPOT - PA_DEPTH)`（否则 acos 得 126.94° 反成大弧）。
  - 恢复备份重生成写回，最终候选 SHA `24a1d5fa…`；写后回读：双侧弧端点 x=±36.0000
    **精确落在禁区横线上**（误差 0），z=±7.312 与理论交点一致，Temp 同步。

## 设计说明

> **玩法与物理设计权威文档**：见 `examples/football/DESIGN.md`（2026-08-22 已确认：
> 单人训练场 / 半场单门 / 官方角色 / 9 项选项卡（6 射门+2 传球+1 停球）/ 完整物理（重力+阻力+
> 马格努斯弧线+反弹扫掠+控球弹簧场）/ 5Hz 匀速直线运动器插值方案）。设计变更须同步本文档+知识树。

### 球场规格
- FIFA 标准 105×68m（长 X 向，宽 Z 向）
- 白线元件的 items 以世界坐标直接写入（元件放 [0,0,0] 即正确位置）
- 线宽 0.12m、厚 0.02m、颜色 0xD0D0D0（浅灰白）
- 默认草地已有（用户说明），白线直接贴地画在草地上

### 球门布局
- 左右球门用同一个元件定义（1077936130），两个实体不同 rotation/position
- 左门：[-52.5, 0, 3.66] rot [0, 90, 0]（开口朝 +X 场内，球网朝 -X 场外）
- 右门：[52.5, 0, -3.66] rot [0, -90, 0]（开口朝 -X 场内，球网朝 +X 场外）
- 球门内宽 7.32m，横梁高 2.5m，球网深 1.8m

### 足球
- 复用「足球」官方预制体（12 五棱柱 + 120 三棱柱 + 92 缝线长条）
- 实体 scale 0.25（直径≈0.5m），中心 [0, 0.247, 0]（y=半径，底面贴地）
- **预制体版本选择**：V4「升级4全开」自带展示底座装饰（球下出现跟随实体），改用 V2/V3 无底座版本，重建预制体 1077936138（222 items，去 2 底座 item）；旧预制体 1077936131（带底座）已弃用未引用

## 证据目录
- `examples/football/evidence/`：plan.json、candidate.gil、preview.json、root-wire-diff.json、entity-import.candidate.gil、entity-root-wire-diff.json
- `~/genshin-ts-evidence/static-assembly/football-real-v1/`（待创建）